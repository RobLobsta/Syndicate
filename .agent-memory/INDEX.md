# Agent Memory Index

**Generated:** 2026-08-13
**Entries:** 174 (active: 150, superseded: 20, resolved: 4)

> This file is generated by `memory-system`. Do not edit by hand.
> Entry format and triggers: docs/13_persistent_memory_system.md#D13-S4.2

## Decisions

| ID | Title | Date | Status | Related Docs |
|---|---|---|---|---|
| [DEC-001](decisions/DEC-001.md) | Own an Ashley-style component model rather than depending on Ashley or Artemis | 2026-08-07 | active | D04-S4.1, D00-S5.2 |
| [DEC-002](decisions/DEC-002.md) | Chose the ray-cast vehicle model over rigid-body wheels | 2026-08-07 | active | D06-S5.5, D05-S5.5 |
| [DEC-003](decisions/DEC-003.md) | glTF 2.0 (.glb) is the runtime mesh format, chosen for morph target support | 2026-08-07 | active | D08-S4.5, D09-S5.6 |
| [DEC-004](decisions/DEC-004.md) | A vehicle is one rigid body with a compound shape, not one body per part | 2026-08-07 | active | D05-S4.1, D06-S5.3 |
| [DEC-005](decisions/DEC-005.md) | Shard trajectories are cosmetic and are never replicated | 2026-08-07 | active | D07-S4.2, D10-S4.3 |
| [DEC-006](decisions/DEC-006.md) | Blueprint invariants are enforced by Gradle check tasks, not by convention | 2026-08-08 | active | D02-S5.6, D12-S5.4 |
| [DEC-007](decisions/DEC-007.md) | ECS interface is EntitySystem; LaunchConfig belongs to shared-models | 2026-08-08 | active | D04-S4.5, D03-S4.2, D02-S4.5 |
| [DEC-008](decisions/DEC-008.md) | The harness parses GLB itself rather than importing it through gdx-gltf | 2026-08-08 | active | D14-S5.13, D02-S4.5 |
| [DEC-009](decisions/DEC-009.md) | MAX_SHARDS_PER_PART raised from 64 to 256 | 2026-08-08 | active | D00-S6.4, D09-S4.2 |
| [DEC-010](decisions/DEC-010.md) | CI runs the D12-S5.4 stages as steps in one job, not one job per stage | 2026-08-08 | active | D12-S5.4, D02-S4.6 |
| [DEC-011](decisions/DEC-011.md) | A non-convex source is decomposed by solid BSP, exactly, not by V-HACD | 2026-08-08 | active | D09-S5.2, D14-S7.1 |
| [DEC-012](decisions/DEC-012.md) | PhysicsWorld is injected into PhysicsSystem, and cross-tick impulses queue on it | 2026-08-08 | active | D06-S5.4, D04-S4.5, D02-S5.7 |
| [DEC-013](decisions/DEC-013.md) | test-environment does not depend on gdx-gltf; D02-S4.5 amended to match | 2026-08-08 | active | D02-S4.5, D14-S5.13 |
| [DEC-014](decisions/DEC-014.md) | An attached part's mass lives in a bodyless RigidBodyComponent | 2026-08-08 | active | D04-S4.3.1, D06-S5.7, D05-S4.4 |
| [DEC-015](decisions/DEC-015.md) | Per-vehicle compounds and their child-index maps are owned by ShapeCache | 2026-08-08 | active | D06-S5.3, D06-S4.3, D02-S5.7 |
| [DEC-016](decisions/DEC-016.md) | detachPart is a shared operation, and slot 15 triggers on its own output | 2026-08-08 | active | D05-S5.5, D06-S5.7, D04-S4.5 |
| [DEC-017](decisions/DEC-017.md) | wreckVehicle does not fracture the chassis; slot 13 does, one slot earlier | 2026-08-08 | active | D07-S5.7, D04-S4.4, D05-S5.5 |
| [DEC-018](decisions/DEC-018.md) | a detached part becomes debris from its own state, not from its trigger | 2026-08-08 | active | D05-S5.5, D07-S5.7, D04-S4.3.1 |
| [DEC-019](decisions/DEC-019.md) | the chassis is a field on the assembly record, not a row in its parts list | 2026-08-09 | active | D08-S4.4, D05-S5.1, D00-S5.1 |
| [DEC-020](decisions/DEC-020.md) | MASS_DELTA_FRAC aliases MASS_TOLERANCE_FRAC, and COM_OFFSET_M is 0.01 m | 2026-08-09 | active | D08-S5.4, D00-S6.4, D06-S4.3 |
| [DEC-021](decisions/DEC-021.md) | the spawn path establishes mass properties itself; slot 15 confirms them | 2026-08-09 | active | D05-S5.2, D06-S5.7, D04-S5.3 |
| [DEC-022](decisions/DEC-022.md) | wheel radius comes from the collision mesh; suspension length is a constant | 2026-08-09 | superseded (by DEC-029) | D06-S5.5, D05-S4.5, D08-S4.2 |
| [DEC-023](decisions/DEC-023.md) | a progress entry tracks one subsystem, and supersession never crosses subsystems | 2026-08-09 | active | D13-S4.1, D13-S5.6, D13-S5.3 |
| [DEC-024](decisions/DEC-024.md) | the degradation table is exhaustive, and a multiplier fades toward identity | 2026-08-09 | active | D05-S5.4, D05-S5.6, D08-S4.2 |
| [DEC-025](decisions/DEC-025.md) | slot 6 recomputes every tick, and the power budget is over live parts | 2026-08-09 | active | D05-S5.6, D05-S5.7, D04-S4.5 |
| [DEC-026](decisions/DEC-026.md) | steering state lives on the chassis, and every vehicle carries a PlayerInput | 2026-08-09 | active | D04-S4.3.2, D04-S4.2, D06-S5.5 |
| [DEC-027](decisions/DEC-027.md) | steer lock, steer rate, drag and rolling resistance are named constants | 2026-08-09 | active | D05-S5.6, D05-S4.5, D08-S4.2 |
| [DEC-028](decisions/DEC-028.md) | a destroyed wheel is commanded to zero, not skipped | 2026-08-09 | active | D06-S5.5, D05-S5.4 |
| [DEC-029](decisions/DEC-029.md) | the wheel tuning defaults are D06-S4.5's, not Bullet's forklift demo's | 2026-08-09 | active | D06-S4.5, D06-S5.5, D05-S4.5 |
| [DEC-030](decisions/DEC-030.md) | the catalogue lives in game-core; client systems arrive via a provider | 2026-08-09 | active | D03-S5.2, D04-S4.4, D02-S5.6 |
| [DEC-031](decisions/DEC-031.md) | a part authors physics it cannot express as a stat in a handling block | 2026-08-09 | active | D08-S4.2, D06-S4.5, D05-S4.5 |
| [DEC-032](decisions/DEC-032.md) | tractive force is capped by engine power, so a vehicle has a top speed | 2026-08-09 | active | D05-S4.5, D05-S5.6, D06-S5.5 |
| [DEC-033](decisions/DEC-033.md) | a vehicle's physics come from a real car; its name does not | 2026-08-09 | active | D05-S5.6, D08-S4.4, D01-S5.7 |
| [DEC-034](decisions/DEC-034.md) | brake force is split across wheels by the load each is carrying | 2026-08-09 | active | D06-S5.5, D05-S5.6 |
| [DEC-035](decisions/DEC-035.md) | the glTF reader lives in game-core and composes node transforms | 2026-08-09 | active | D08-S4.5, D02-S4.5, D14-S5.13 |
| [DEC-036](decisions/DEC-036.md) | a model's unit and axis correction lives in import.json, verified | 2026-08-09 | active | D08-S4.1, D00-S5.2, D14-S5.3 |
| [DEC-037](decisions/DEC-037.md) | the Stampede comes from a Mustang GTD, so the pair's contrast is mass | 2026-08-09 | active | D05-S5.6, D08-S4.4, D01-S5.7 |
| [DEC-038](decisions/DEC-038.md) | the damage pipeline is a shared operation, and slot 12 is only its schedule slot | 2026-08-10 | active | D07-S5.2, D07-S5.9, D04-S4.5 |
| [DEC-039](decisions/DEC-039.md) | a weapon's identity is a block; its numbers are stats | 2026-08-10 | active | D08-S4.2, D01-S4.4, D05-S4.5 |
| [DEC-040](decisions/DEC-040.md) | the damage ledger is a bucketed window on the match singleton | 2026-08-10 | active | D01-S5.4, D04-S4.3.5 |
| [DEC-041](decisions/DEC-041.md) | the asset pipeline validates independently of the runtime loader | 2026-08-10 | active | D08-S5.2, D08-S5.4, D02-S4.5 |
| [DEC-042](decisions/DEC-042.md) | a vehicle is dissected by geometry, never by the names in the file | 2026-08-10 | active | D08-S4.1, D08-S4.2, D09-S4.1 |
| [DEC-043](decisions/DEC-043.md) | a part's mass acts at its collision mesh's centre, not at its slot | 2026-08-10 | active | D06-S5.7, D08-S4.2, D05-S5.2 |
| [DEC-044](decisions/DEC-044.md) | what turns with a wheel is decided by rotational symmetry | 2026-08-10 | active | D15-S5.4, D06-S5.5 |
| [DEC-045](decisions/DEC-045.md) | the material says what a part is made of; the part says how it fails | 2026-08-10 | active | D08-S4.3, D15-S5.7, D15-S8 |
| [DEC-046](decisions/DEC-046.md) | the sound bank is synthesised, and the reason is the licence | 2026-08-10 | active | D15-S8, D08-S4.6 |
| [DEC-047](decisions/DEC-047.md) | an engine is keyed on its configuration, not on its vehicle's weight class | 2026-08-10 | active | D15-S8, D05-S5.6 |
| [DEC-048](decisions/DEC-048.md) | gamepad and keyboard are peers, and the active one is observed | 2026-08-10 | active | D04-S4.4, D03-S4.1 |
| [DEC-049](decisions/DEC-049.md) | a PRESENT system is handed frame time, and reads alpha off the world | 2026-08-11 | active | D03-S5.3, D04-S4.5, D07-S5.5 |
| [DEC-050](decisions/DEC-050.md) | slot 22 interpolates the ticks this process ran, not snapshots from a wire | 2026-08-11 | active | D03-S5.3, D10-S5.6, D04-S4.3.5 |
| [DEC-051](decisions/DEC-051.md) | the client attaches its own cosmetic components; core factories never do | 2026-08-11 | active | D04-S4.2, D03-S5.5, D07-S5.5 |
| [DEC-052](decisions/DEC-052.md) | an engine is a pulse train through an exhaust, not a harmonic stack | 2026-08-12 | active | D15-S8, D05-S5.6 |
| [DEC-053](decisions/DEC-053.md) | induction is a second voice, keyed on a closed set like the configuration is | 2026-08-12 | active | D15-S8, D05-S5.6 |
| [DEC-054](decisions/DEC-054.md) | bank divergence scales with how unevenly a bank fires | 2026-08-12 | active | D15-S8, D05-S5.6 |
| [DEC-055](decisions/DEC-055.md) | engines get their own audio device and mixer, not libGDX Sound handles | 2026-08-12 | active | D15-S8, D02-S4.5 |
| [DEC-056](decisions/DEC-056.md) | the engine is synthesised at runtime; 27 of the bank's 74 files are deleted | 2026-08-12 | active | D15-S8, D04-S4.4 |
| [DEC-057](decisions/DEC-057.md) | a V's gain imbalance scales with bank unevenness and has no floor | 2026-08-13 | active | D15-S8 |
| [DEC-058](decisions/DEC-058.md) | the client's acknowledgement rides on its input, and a NACK is a new message | 2026-08-13 | active | D10-S4.2 |
| [DEC-059](decisions/DEC-059.md) | a vehicle's network ids are one contiguous block, derived not sent | 2026-08-13 | active | D04-S6.2, D10-S4.2 |
| [DEC-060](decisions/DEC-060.md) | a part is exactly as relevant as the vehicle it is attached to | 2026-08-13 | active | D10-S5.10 |
| [DEC-061](decisions/DEC-061.md) | vehicle control becomes a shared operation so reconciliation can replay it | 2026-08-13 | active | D06-S5.5, D10-S5.5, D04-S4.5 |
| [DEC-062](decisions/DEC-062.md) | a client sends its input from slot 19, because slot 18 is authority-only | 2026-08-13 | active | D04-S4.4, D10-S5.5 |
| [DEC-063](decisions/DEC-063.md) | D15's slot roles are not SlotTypes, and a door is an armour panel | 2026-08-13 | active | D15-S4.1, D05-S4.3, D08-S4.2 |
| [DEC-064](decisions/DEC-064.md) | a prepared vehicle is weighed from its footprint; the chassis takes the balance | 2026-08-13 | superseded (by DEC-067) | D15-S5.8, D05-S5.7, D08-S5.4 |
| [DEC-065](decisions/DEC-065.md) | the pipeline writes import.json, and writes the composition not the residual | 2026-08-13 | active | D15-S5.5, D08-S4.1 |
| [DEC-066](decisions/DEC-066.md) | a piece turns with a wheel if it covers the circle **or** repeats around it | 2026-08-13 | active | D15-S5.4, D06-S5.5 |
| [DEC-067](decisions/DEC-067.md) | a part's mass is its surface times a per-class areal density | 2026-08-13 | active | D15-S5.8, D09-S6.2 |

### DEC-001 — Own an Ashley-style component model rather than depending on Ashley or Artemis
The simulation uses an in-house, component-based data-oriented model with a fixed explicit system schedule, rather than adopting Ashley (libGDX's ECS) or Artemis-odb.

### DEC-002 — Chose the ray-cast vehicle model over rigid-body wheels
Vehicles use `btRaycastVehicle` — one rigid body plus N ray-cast wheels — rather than rigid-body wheels joined by `btHinge2Constraint`.

### DEC-003 — glTF 2.0 (.glb) is the runtime mesh format, chosen for morph target support
Processed meshes ship as glTF 2.0 binary (`.glb`), imported with gdx-gltf, because it is the only well-supported format that carries morph targets as a first-class feature.

### DEC-004 — A vehicle is one rigid body with a compound shape, not one body per part
Each vehicle is a single `btRigidBody` whose collision shape is a `btCompoundShape` of per-part convex hulls. Parts become independent bodies only at the moment they detach.

### DEC-005 — Shard trajectories are cosmetic and are never replicated
The authority replicates the *fact* of a fracture (part, tick, parent velocity) but never individual shard transforms or velocities. Each client spawns its own shard set locally.

### DEC-006 — Blueprint invariants are enforced by Gradle check tasks, not by convention
Every mechanically checkable rule in D02 and D00 is implemented as a Gradle task in `build-logic/` and wired into `check`, rather than left as a documented convention. The module catalogue of D02-S4.5 is duplicated as data in `ModuleRules.kt` so the checks have something to compare against.

### DEC-007 — ECS interface is EntitySystem; LaunchConfig belongs to shared-models
Two placements the blueprints left open: the ECS system interface is `EntitySystem`, not `System`, and `LaunchConfig` plus its resolver live in `shared-models` (`dev.syndicate.model.config`) rather than in `game-core`.

### DEC-008 — The harness parses GLB itself rather than importing it through gdx-gltf
`test-environment` reads vertex positions and indices out of `.glb` with its own reader (`GlbReader`, ~200 lines) instead of loading the file through gdx-gltf. The decisive reason is that gdx-gltf builds libGDX `Mesh` objects, which are GPU buffers requiring a GL context — and D14-S5.13 requires the headless runner to create none.

### DEC-009 — MAX_SHARDS_PER_PART raised from 64 to 256
The hard cap on shards per part goes from 64 to 256. The old value was chosen before there was a fracture implementation to measure; the exact half-space path makes a 100-shard fracture cost 2 seconds and hold every correctness property, so the cap was bounding nothing real.

### DEC-010 — CI runs the D12-S5.4 stages as steps in one job, not one job per stage
The workflow ran three jobs (stages 0, 1-2, 7) and triggered on both `push: ["**"]` and `pull_request`, so every commit on a branch with an open PR ran the whole pipeline twice: 6 jobs, 573 s of runner time, for one commit's worth of information. It is now a single job whose steps are the stages, triggered on `pull_request` plus `push` to `main` only.

### DEC-011 — A non-convex source is decomposed by solid BSP, exactly, not by V-HACD
A non-convex source is split into disjoint convex pieces by a solid binary space partition over its own face planes, so `cell ∩ source` becomes a union of exact polytope intersections. An approximate decomposition (V-HACD) and a per-cell mesh boolean were both rejected: each gives up the exactness that is the point of the convex path.

### DEC-012 — PhysicsWorld is injected into PhysicsSystem, and cross-tick impulses queue on it
`PhysicsWorld` is a constructor dependency of `PhysicsSystem` rather than a field on `World`, and the "pending impulses" of D06-S5.4 step 1 are a queue owned by `PhysicsWorld`, drained and applied by `PhysicsSystem` in ascending entity id order.

### DEC-013 — test-environment does not depend on gdx-gltf; D02-S4.5 amended to match
`gdx-gltf` is removed from `test-environment`'s dependencies and moved to its "MUST NOT depend on" column in D02-S4.5. No source in the repository ever imported it — DEC-008 made it unused — and it was the one artifact keeping the module from building without JitPack.

### DEC-014 — An attached part's mass lives in a bodyless RigidBodyComponent
A part that is attached to a vehicle carries a `RigidBodyComponent` whose `body` is null and whose `massKg` and `centerOfMassLocal` are its authoritative mass properties. `MassPropertySystem` sums those fields; nothing else in the component catalogue holds a part's mass at runtime.

### DEC-015 — Per-vehicle compounds and their child-index maps are owned by ShapeCache
A vehicle's `btCompoundShape` and its slot-path-to-child-index map live in a `VehicleCompound` registered in `ShapeCache` under the key `(assemblyId, COMPOUND, entityIndex)`, and are released by `EntityDestroySystem` when the vehicle dies. They are the one instance-scoped, mutable entry in a cache that is otherwise immutable and shared.

### DEC-016 — detachPart is a shared operation, and slot 15 triggers on its own output
`detachPart` lives in `dev.syndicate.core.vehicle.PartDetachment` as a static operation over components, called by both `FractureSystem` (13) and the future `DetachSystem` (14). It does **not** call `MassPropertySystem.recompute`: slot 15 detects the change itself, by comparing the mass and COM it computes against the values stored on `VehicleChassisComponent`.

### DEC-017 — wreckVehicle does not fracture the chassis; slot 13 does, one slot earlier
D07-S5.7's `wreckVehicle` calls `fracturePart` on the chassis, which D04-R13 prohibits as a system-to-system call. `FractureSystem` (13) instead fractures a destroyed chassis part like any other — without detaching it, because the chassis cannot be detached (D05-R26) — and `DetachSystem` (14) treats a chassis part that is *gone or destroyed* as the wreck trigger, one slot later in the same tick.

### DEC-018 — a detached part becomes debris from its own state, not from its trigger
`PartDetachment.detach` records each leaving part's world transform and the velocity it left with (`v + ω × r`) on the part's own `Transform` and `Velocity`, and `DetachSystem` (14) turns every part that is `DETACHED`, vehicle-less and bodyless into a debris body in one pass keyed on that state. Neither half asks which trigger removed the part, or whether slot 14 removed it at all.

### DEC-019 — the chassis is a field on the assembly record, not a row in its parts list
`AssemblyDef` carries `chassisPartTypeId` as its own field and lists only the attached parts in `parts`, following D08-S4.4's file format rather than D05-S5.1's validation pseudocode, which reads the root out of `parts` by looking for a null `parentSlotPath`.

### DEC-020 — MASS_DELTA_FRAC aliases MASS_TOLERANCE_FRAC, and COM_OFFSET_M is 0.01 m
D08 names two tolerance constants — `MASS_DELTA_FRAC` and `COM_OFFSET_M` — that D00-S6.4 never defines. `AssemblyValidator.MASS_DELTA_FRAC` is an alias of `MASS_TOLERANCE_FRAC` (0.02), and `COM_OFFSET_M` is fixed at 0.01 m, one collision margin.

### DEC-021 — the spawn path establishes mass properties itself; slot 15 confirms them
`VehicleFactory` computes total mass and centre of mass, recentres the compound on the COM, and writes both to `VehicleChassisComponent` and `RigidBodyComponent` as part of building the chassis body — rather than calling `MassPropertySystem.recompute` as D05-S5.2 step 4 does. Slot 15 then compares equal on the spawn tick and does nothing.

### DEC-022 — wheel radius comes from the collision mesh; suspension length is a constant
`btRaycastVehicle.addWheel` needs a wheel radius and a suspension rest length, and no blueprint authors either. The radius is derived from the wheel's collision mesh — half the larger of its Y and Z extents — and the rest length is a constant, `WHEEL_SUSPENSION_REST_LENGTH_M = 0.3`.

### DEC-023 — a progress entry tracks one subsystem, and supersession never crosses subsystems
`progress/` holds one active entry per subsystem — simulation, destruction toolchain, content and runtime shells — and a new entry supersedes only the entry for the same subsystem. PROG-001 to PROG-006 formed a single global chain instead, which had already hidden a working subsystem behind a superseded entry.

### DEC-024 — the degradation table is exhaustive, and a multiplier fades toward identity
A `(category, stat)` pair the D05-S5.4 DEGRADATION_TABLE does not name does not degrade, and a stat's `mul` term fades toward 1 rather than toward 0: `mul' = 1 + (mul - 1) · m`. Both are questions D05-S5.4 leaves open once its scalar pseudocode meets `StatBlock`'s add/mul pairs.

### DEC-025 — slot 6 recomputes every tick, and the power budget is over live parts
`VehicleStatsSystem` aggregates every vehicle on every tick rather than gating on `VehicleStats.dirty` or on a remembered `structuralVersion`, and its `powerBudget` sums the live parts rather than the whole assembly.

### DEC-026 — steering state lives on the chassis, and every vehicle carries a PlayerInput
`VehicleChassisComponent` gains `currentSteerRad` and `VehicleStatsComponent` gains `maxSteerRad`; D04-S4.3.2 was amended in the same commit for both. `VehicleFactory` also adds a `PlayerInputComponent` to every vehicle, which D04-R4's `VEHICLE` archetype requires and D05-S5.2's pseudocode omits.

### DEC-027 — steer lock, steer rate, drag and rolling resistance are named constants
Four numbers D05-S5.6 needs and no document authors: `DEFAULT_MAX_STEER_RAD = 0.5236` (30°), `DEFAULT_STEER_RATE_RAD_PER_SEC = 1.05`, `CHASSIS_DRAG_COEFFICIENT = 12.0` N per (m/s)², and `CHASSIS_ROLLING_RESISTANCE = 0.015`. All four live on `VehicleStatsSystem` with the reasoning below.

### DEC-028 — a destroyed wheel is commanded to zero, not skipped
D06-S5.5's control loop reads `if w.destroyed: continue`. `VehicleControlSystem` instead writes zero engine force, zero steering and zero grip to a destroyed wheel, because Bullet's per-wheel setters are persistent rather than per-step.

### DEC-029 — the wheel tuning defaults are D06-S4.5's, not Bullet's forklift demo's
`VehicleFactory`'s per-wheel constants now carry D06-S4.5's reference chassis values. DEC-022 chose Bullet demo figures on the reading that no blueprint authored them; D06-S4.5's R12 table authors all of them.

### DEC-030 — the catalogue lives in game-core; client systems arrive via a provider
`SystemSlot` holds all 27 slots of D04-S4.4 with their phase, module and mode filter; `SystemSetFactory.forMode` filters it and asks a `SystemProvider` to construct each surviving slot. `CoreSystemProvider` builds the `game-core` systems; the six `game-client` slots are contributed by a provider the client passes in.

### DEC-031 — a part authors physics it cannot express as a stat in a handling block
`HandlingBlock` carries the nine physical parameters D06-S4.5's reference chassis table names and D05-S4.5's stat list cannot hold: drag, rolling resistance and downforce on a chassis; damping, roll influence and suspension geometry on a wheel. It is authored as a `handling` object on the part, and D08-R5 was amended in the same commit.

### DEC-032 — tractive force is capped by engine power, so a vehicle has a top speed
A fifteenth stat, `enginePowerW`, joins D05-S4.5's table. Available tractive force is `min(engineForceN, enginePowerW / v)`: traction-limited at rest, power-limited once moving. Without it, a chassis calibrated to a real car's 0-100 time reports a top speed several times what that car has.

### DEC-033 — a vehicle's physics come from a real car; its name does not
`VehicleProfile` carries a `displayName` for the in-game vehicle and a separate `referenceVehicle` naming the real car its figures came from. The Apex GT is derived from a Maserati MC20 and the Stampede GT3 from a Ford Mustang GT3; neither manufacturer's name appears in an asset id, a display name or the shipped content.

### DEC-034 — brake force is split across wheels by the load each is carrying
D06-S5.5 divides brake force equally across the wheels. `VehicleControlSystem` divides it in proportion to each wheel's live suspension load instead, which is what an electronic brake-force distribution does and what makes a shipped vehicle stop in roughly its published distance.

### DEC-035 — the glTF reader lives in game-core and composes node transforms
`GltfReader` is a `game-core` class in `dev.syndicate.core.asset`, it composes every node's transform down from the scene root and applies it to the geometry it returns, and `test-environment`'s `GlbReader` becomes a thin adapter over it rather than a second parser.

### DEC-036 — a model's unit and axis correction lives in import.json, verified
The correction that takes a piece of downloaded art into the game's units and axes is recorded once, in an `import.json` beside the model — scale, then yaw about Y, then translation — and the harness's `--model` mode applies it *before* it measures, so the numbers in that file are confirmed by the checks rather than asserted by a comment.

### DEC-037 — the Stampede comes from a Mustang GTD, so the pair's contrast is mass
`STAMPEDE`'s reference car changes from the 1289 kg Ford Mustang GT3 to the 1969 kg Ford Mustang GTD, because the GTD is the car the supplied art actually is. The shipped pair is therefore a light car against a heavy one rather than a road car against a race car, and the calibration tests assert the new contrast rather than the old.

### DEC-038 — the damage pipeline is a shared operation, and slot 12 is only its schedule slot
The armour formulas, the damage state machine and the propagation walk live in `dev.syndicate.core.damage.DamageApplication`, a static-shaped operation over components in the mould of `PartDetachment` (DEC-016). `DamageSystem` (12) owns the schedule slot, the event ordering and the burn timer, and nothing else.

### DEC-039 — a weapon's identity is a block; its numbers are stats
`WeaponBlock` carries what a weapon *is* — family, damage type, ammunition capacity, blast radius, range, muzzle offset — and D05-S4.5's stat block keeps what a weapon *does*: fire interval, damage per shot, spread, heat, projectile speed. D08-R5 was amended in the same commit, exactly as it was for `HandlingBlock` (DEC-031).

### DEC-040 — the damage ledger is a bucketed window on the match singleton
`DamageLedger` keeps a match-long total and a ten-second window per attacker-victim pair, the window as ten fixed buckets of sixty ticks rather than a log of every hit. It lives in a `DamageLedgerComponent` on the match singleton (entity 1), and D04-S4.3.5 was amended to name it.

### DEC-041 — the asset pipeline validates independently of the runtime loader
`asset-pipeline`'s `AssetIndexBuilder` reads `assets/` with its own Jackson walk rather than calling `game-core`'s `AssetLoader`. The two share the D08-S5.4 rule codes and nothing else.

### DEC-042 — a vehicle is dissected by geometry, never by the names in the file
`syndicate_dissect` finds a vehicle's wheels by connected-component analysis and spatial classification — outboard, low, and round in profile — and ignores every name in the source file. It is a second Blender package beside `syndicate_fracture` rather than a mode of it.

### DEC-043 — a part's mass acts at its collision mesh's centre, not at its slot
`PartType.centerOfMassLocal` is the AABB centre of the part's collision mesh, and both `AssemblyLayout` and `MassPropertySystem` (15) weight a part's mass there rather than at its attachment point. D06-S5.7's pseudocode already says `part.type.comLocal`; the implementation was using the slot transform's translation, which is a different point on every part that is not centred on its own origin.

### DEC-044 — what turns with a wheel is decided by rotational symmetry
A piece captured into a wheel corner rotates with the wheel only if its geometry occupies at least `ROTATION_SYMMETRY_MIN_DEG` (300°) of the circle about the axle, measured over vertices in 15° sectors. Everything else — the brake caliper — is returned to the chassis.

### DEC-045 — the material says what a part is made of; the part says how it fails
`assets/materials/materials.json` is the single shared authority for density, per-damage-type resistance, fracture brittleness and — new — `audioMaterial`. `destructionClass` is **not** on it: it is a property of what a part *is*, so it lives on `part.json` and defaults from `PartCategory`.

### DEC-046 — the sound bank is synthesised, and the reason is the licence
Every sound in `assets/audio/` is generated by `asset-pipeline`'s `SoundBankBuilder` rather than sampled or downloaded. D15-R38 ranks procedural synthesis second behind permissively licensed libraries; the bank takes it first, for every family rather than only the loops.

### DEC-047 — an engine is keyed on its configuration, not on its vehicle's weight class
Engine loops are generated one per `EngineConfiguration` — I4, I6, V6, V8, V10, V12 — and a vehicle's own `EngineVoice` (configuration, idle rpm, redline rpm, peak power) decides the pitch, gain and low-end weight it is played at. The first cut keyed them on `light`/`medium`/`heavy` and was wrong.

### DEC-048 — gamepad and keyboard are peers, and the active one is observed
`InputCollectionSystem` (slot 1) polls both a `GamepadSource` and a `KeyboardMouseSource` every frame and keeps the output of whichever device the player last touched. Neither is primary. Tuning is content, in `assets/input/bindings.json`.

### DEC-049 — a PRESENT system is handed frame time, and reads alpha off the world
`World.present(alpha, frameDtSeconds)` passes the real frame delta as a PRESENT system's `dtSeconds` and exposes the render interpolation factor separately through `World.renderAlpha()`. The single-argument `present(alpha)`, which passed alpha as the dt, is gone.

### DEC-050 — slot 22 interpolates the ticks this process ran, not snapshots from a wire
`InterpolationSystem` (slot 22) samples every entity's world transform once per tick and rebuilds a render transform `alpha` of the way between the last two samples. It does not read `InterpolationComponent`'s snapshot ring buffer, which stays empty until slot 19 exists to fill it.

### DEC-051 — the client attaches its own cosmetic components; core factories never do
`RenderTransformComponent`, `RenderModelComponent` and `DamageVisualComponent` are attached by the client systems that own them — slots 22, 26 and 23 respectively — each through a family that excludes the component it is about to add. `VehicleFactory` and `DebrisFactory` attach none of them.

### DEC-052 — an engine is a pulse train through an exhaust, not a harmonic stack
`SoundSynth.engineLoop` builds an engine from one unipolar exhaust pulse per firing event, placed at its crank angle and routed to its own bank, then run through three fixed exhaust formants — rather than stacking harmonics of the firing frequency. How far a V's two banks diverge is derived from `EngineConfiguration.bankFiringSpreadDegrees()`, not from a global constant. See DEC-054 for that second half.

### DEC-053 — induction is a second voice, keyed on a closed set like the configuration is
A new `Induction` enum — `NATURALLY_ASPIRATED`, `TURBO`, `SUPERCHARGED` — joins `EngineConfiguration` on `EngineVoice`. The bank holds one loop per forced member plus a release for the one that has one; the runtime derives pitch and gain per vehicle. D15-S8 was amended in the same commit (R36a–c).

### DEC-054 — bank divergence scales with how unevenly a bank fires
`SoundSynth.bankDivergence` returns 0.10 for an arrangement whose banks fire identically and 1.0 for the cross-plane V8, interpolated on `bankFiringSpreadDegrees() / 180`. It scales both the second bank's formant detune and its manifold delay. A global constant does not work, and the two ways it fails are opposite.

### DEC-055 — engines get their own audio device and mixer, not libGDX Sound handles
Engine voices are rendered into `EngineMixer` and written to a `Gdx.audio.newAudioDevice(48000, false)` stereo sink on a dedicated daemon thread. Every other sound in the bank stays on libGDX `Sound` exactly as it was. Positioning — distance, pan, air absorption, propagation delay and the doppler that falls out of it — is ours, applied inside the mixer.

### DEC-056 — the engine is synthesised at runtime; 27 of the bank's 74 files are deleted
`EngineSynth` in `game-client` integrates the crank angle forward at the simulation's rpm and fires cylinders as they come round, a block at a time from the audio callback. Six engine loops, eighteen start/stop/overrun one-shots and three induction files are deleted; the bank goes 74 → 47. `EngineVoice` loses its sound ids, its pitch ratio and `REFERENCE_RPM`; `AudioEvent` loses six members.

### DEC-057 — a V's gain imbalance scales with bank unevenness and has no floor
The detune and the manifold delay between a V's banks keep their 10% floor, so no two banks are ever identical. The **gain** imbalance loses it: it now scales purely with how unevenly the banks fire, which is zero for every arrangement here except the cross-plane V8.

### DEC-058 — the client's acknowledgement rides on its input, and a NACK is a new message
`InputCommand` gains an `acknowledgedSnapshotTick` field, and a new `SnapshotNack` message joins the D10-S4.2 catalogue. D10-S4.2 was amended in the same commit.

### DEC-059 — a vehicle's network ids are one contiguous block, derived not sent
`SpawnEntity` carries a base network id and a block size. The vehicle takes the base, its chassis part the next id, and every other part the ids after that in ascending slot-path order — a walk both peers perform for themselves in `ReplicationTagging.replicatedEntities`.

### DEC-060 — a part is exactly as relevant as the vehicle it is attached to
`Relevance.isRelevantTo` answers for a part by asking the same question of its vehicle, rather than measuring the part's own distance or treating it as an unknown archetype.

### DEC-061 — vehicle control becomes a shared operation so reconciliation can replay it
The body of `VehicleControlSystem` (slot 7) moves to `dev.syndicate.core.vehicle.VehicleControl`. The system keeps the slot, the family and its public constants; `ReconciliationSystem` (20) holds its own instance.

### DEC-062 — a client sends its input from slot 19, because slot 18 is authority-only
`NetworkReceiveSystem` (19) records the tick's prediction, sends the `InputCommand` packet, and then applies whatever arrived. D04-S4.4's row for slot 19 was amended in the same commit to say so.

### DEC-063 — D15's slot roles are not SlotTypes, and a door is an armour panel
The preparation pipeline maps each D15-S4.1 label onto a real `PartCategory` and a real `SlotType`, and D15-S4.1 was amended in the same commit to carry both columns. A `panel` — which is what a door, a bonnet and a bumper all are — is exported as an `ARMOR` part in an `ARMOR_PANEL` slot.

### DEC-064 — a prepared vehicle is weighed from its footprint; the chassis takes the balance
A dropped-in model has a kerb mass nobody measured and a power budget nobody authored. The pipeline derives both: mass per part from `area × wall thickness × density` (DISC-035) with the **chassis taking whatever is left** of a target derived from the vehicle's footprint, and `powerCost` **distributed** across the parts so the assembly's budget equals its class's target exactly.

### DEC-065 — the pipeline writes import.json, and writes the composition not the residual
`syndicate-prepare` derives the scale, yaw and translation that put a model in the game's frame and records them in the model's own `import.json`. DEC-036 stands unchanged — that file is still the single recorded correction and still what the harness verifies — but an operator no longer has to author it before the pipeline will run.

### DEC-066 — a piece turns with a wheel if it covers the circle **or** repeats around it
DEC-044's angular-coverage test gains a second, independently sufficient one: a material group in a wheel corner also rotates if its occupied sectors are invariant under a turn of `360/n` for some `n ≥ 3`. Neither test replaces the other; a piece rotates if it passes either.

### DEC-067 — a part's mass is its surface times a per-class areal density
The per-class mass constant is a **mass per square metre**, not a wall thickness multiplied by the material's density, and the enclosed volume caps the result. DEC-064's mass formula is superseded; everything else in that entry — the chassis taking the balance, the footprint target, the distributed power budget — stands unchanged.

## Discoveries

| ID | Title | Date | Status | Related Docs |
|---|---|---|---|---|
| [DISC-001](discoveries/DISC-001.md) | Configuration cache rejects onlyIf lambdas reading a script-level val | 2026-08-08 | active | D02-S4.6, D12-S5.4 |
| [DISC-002](discoveries/DISC-002.md) | Blender writes to stdout at the C level, breaking the tool's JSON-only contract | 2026-08-08 | active | D09-S4.1 |
| [DISC-003](discoveries/DISC-003.md) | bpy module teardown segfaults, overwriting the tool's exit code | 2026-08-08 | active | D09-S4.3 |
| [DISC-004](discoveries/DISC-004.md) | btShapeHull.buildHull(margin) double-counts the collision margin | 2026-08-08 | superseded (by DISC-008) | D06-S4.3, D14-S4.5.2 |
| [DISC-005](discoveries/DISC-005.md) | an unanchored `build/` in .gitignore silently untracked build-logic | 2026-08-08 | active | D02-S4.4, D12-S5.4 |
| [DISC-006](discoveries/DISC-006.md) | the point-in-mesh ray test double-counts a ray grazing a shared edge | 2026-08-08 | active | D09-S5.2 |
| [DISC-007](discoveries/DISC-007.md) | the sandbox cannot provision a JDK 17 toolchain; api.foojay.io is blocked | 2026-08-08 | active | D02-S4.1, D12-S5.4 |
| [DISC-008](discoveries/DISC-008.md) | btShapeHull bakes the source shape's margin into its points | 2026-08-08 | active | D06-S5.2, D14-S5.5 |
| [DISC-009](discoveries/DISC-009.md) | btShapeHull has no target vertex count; it reduces to 42 and no further | 2026-08-08 | active | D06-S5.2, D06-S4.3, D09-S5.5 |
| [DISC-010](discoveries/DISC-010.md) | Bullet freezes a body's deactivation clock the moment it falls asleep | 2026-08-09 | active | D07-S5.8, D06-S5.10 |
| [DISC-011](discoveries/DISC-011.md) | Bullet ray tests are locked to DefaultFilter, so a wheel finds no ground | 2026-08-09 | active | D06-S4.4, D06-S5.5 |
| [DISC-012](discoveries/DISC-012.md) | Bullet reads setBrake as an impulse and applyEngineForce as a force | 2026-08-09 | active | D06-S5.5, D06-S5.6 |
| [DISC-013](discoveries/DISC-013.md) | a test resolving LaunchConfig from System.getenv asserts the machine | 2026-08-09 | active | D03-S4.2, D12-S5.4 |
| [DISC-014](discoveries/DISC-014.md) | a published braking ordering does not survive a change of mass | 2026-08-09 | active | D06-S5.5, D05-S5.6 |
| [DISC-015](discoveries/DISC-015.md) | a Bullet contact point carries the compound child index in getIndex0/getIndex1 | 2026-08-10 | active | D07-S5.1, D06-S5.3 |
| [DISC-016](discoveries/DISC-016.md) | a skinned mesh's node transform is not its placement | 2026-08-10 | active | D08-S4.5, D14-S5.3, D02-S4.5 |
| [DISC-017](discoveries/DISC-017.md) | Bullet ray tests a big convex box to about 14 cm | 2026-08-10 | active | D06-S4.3, D06-S5.5, D06-S5.2 |
| [DISC-018](discoveries/DISC-018.md) | an object in a downloaded car is a material group, not a part | 2026-08-10 | active | D15-S5.2, D08-S4.1 |
| [DISC-019](discoveries/DISC-019.md) | one shipped car names its materials, the other does not | 2026-08-10 | active | D15-S4.2, D15-S4.3 |
| [DISC-020](discoveries/DISC-020.md) | assets/ was not a test input, so content changes never re-ran the tests | 2026-08-10 | active | D12-S5.4, D08-S5.3 |
| [DISC-021](discoveries/DISC-021.md) | a guardrail reading project.rootDir fails only when it finds a violation | 2026-08-11 | active | D02-S4.6, D12-S5.4 |
| [DISC-022](discoveries/DISC-022.md) | a same-tick event is consumed before presentation runs, so every hit was silent | 2026-08-11 | active | D04-S4.4, D07-S5.1, D07-S5.9 |
| [DISC-023](discoveries/DISC-023.md) | a crossfade removes a loop's value discontinuity and hides its phase one | 2026-08-12 | active | D15-S8, D12-S4.1 |
| [DISC-024](discoveries/DISC-024.md) | jitpack.io is blocked by the sandbox proxy, so game-client cannot be built here | 2026-08-12 | active | D02-S4.1, D12-S5.4 |
| [DISC-025](discoveries/DISC-025.md) | a parallel band-pass bank with no dry path is a 25 dB gate, not an exhaust | 2026-08-12 | active | D15-S8, D12-S4.1 |
| [DISC-026](discoveries/DISC-026.md) | measured against real engines, the synthesiser was too dark and far too clean | 2026-08-12 | active | D15-S8 |
| [DISC-027](discoveries/DISC-027.md) | an engine's periodic parts must all be tied to the crank, or it reads as a fault | 2026-08-12 | active | D15-S8 |
| [DISC-028](discoveries/DISC-028.md) | a real V8 recording says the even orders were 14 dB light, and why | 2026-08-13 | active | D15-S8 |
| [DISC-029](discoveries/DISC-029.md) | the ignition timing was wrong in every dimension, against a real start | 2026-08-13 | active | D15-S8 |
| [DISC-030](discoveries/DISC-030.md) | the rumble is an envelope ratio, and the sub-orders were never the problem | 2026-08-13 | active | D15-S8 |
| [DISC-031](discoveries/DISC-031.md) | fusing exhaust pulses with a comb re-creates the defect it is fixing | 2026-08-13 | active | D15-S8 |
| [DISC-032](discoveries/DISC-032.md) | a clip of several cars was used as a single-engine reference | 2026-08-13 | active | D15-S8 |
| [DISC-033](discoveries/DISC-033.md) | a matching sub-order total hid a lope in the wrong place | 2026-08-13 | active | D15-S8 |
| [DISC-034](discoveries/DISC-034.md) | the overrun crackle ran off a clock and sounded like popcorn | 2026-08-13 | active | D15-S8 |
| [DISC-035](discoveries/DISC-035.md) | `volume × density` is the wrong mass rule for vehicle art, by 25× | 2026-08-13 | active | D09-S6.2, D15-S5.8 |
| [DISC-036](discoveries/DISC-036.md) | a bearing of exactly zero lands in the last sector, not the first | 2026-08-13 | active | D15-S5.4, D15-R24b |
| [DISC-037](discoveries/DISC-037.md) | a material name defined an axle, and took a third of the car with it | 2026-08-13 | active | D15-S5.4, D15-R23a |
| [DISC-038](discoveries/DISC-038.md) | real art's topology decides what every later stage can do | 2026-08-13 | active | D15-S5.5, D15-S5.2 |
| [DISC-039](discoveries/DISC-039.md) | the fracture tool cannot cut a windscreen, and says so seven different ways | 2026-08-13 | active | D09-S5.2, D15-S5.7 |

### DISC-001 — Configuration cache rejects onlyIf lambdas reading a script-level val
With `org.gradle.configuration-cache=true`, an `onlyIf { }` spec in a `.gradle.kts` file fails to serialise if its body reads a top-level `val` or an unqualified `logger`. The fix is to copy the value into a local inside the task's configure block; the diagnostic never says this.

### DISC-002 — Blender writes to stdout at the C level, breaking the tool's JSON-only contract
D09-R2 requires the tool's stdout to carry exactly one JSON document and nothing else. Blender does not cooperate: its glTF exporter writes a Draco availability notice to the process's stdout at the C level on every run, landing in the middle of the document an agent is about to parse. The tool now duplicates the real stdout to a private descriptor and points fd 1 at stderr.

### DISC-003 — bpy module teardown segfaults, overwriting the tool's exit code
When the tool runs on the `bpy` PyPI module (DEV-002), normal interpreter shutdown after a run that loaded and freed meshes segfaults inside Blender's own teardown. The process then exits 139 regardless of what the tool decided, destroying the exit-code contract D09-S4.3 exists to provide. The entry point calls `os._exit` after flushing instead.

### DISC-004 — btShapeHull.buildHull(margin) double-counts the collision margin
Passing a shape's collision margin to `btShapeHull.buildHull(margin)` offsets the generated hull points outward by that margin, and the `btConvexHullShape` built from the result then adds its own margin on top. A simplified hull therefore has its collision surface two margins outside its mesh while an unsimplified one has exactly one. Pass `0`.

### DISC-005 — an unanchored `build/` in .gitignore silently untracked build-logic
`.gitignore` carried a bare `build/`. Git matches an unanchored pattern against a directory of that name at **any depth**, and `build-logic`'s guardrail tasks live in package `dev.syndicate.build` — so `build-logic/src/main/kotlin/dev/syndicate/build/` was ignored and all seven task classes were never committed. The working tree built perfectly; a clean clone failed to configure with `Unresolved reference: dev`.

### DISC-006 — the point-in-mesh ray test double-counts a ray grazing a shared edge
`fracture._inside_predicate` counts a `+X` ray crossing by triangle parity, and a ray passing exactly along the edge two triangles share is counted **twice** — so the point reads as outside when it is inside. Its docstring claims a tie-break resolves this; there is none.

### DISC-007 — the sandbox cannot provision a JDK 17 toolchain; api.foojay.io is blocked
In the remote agent sandbox the only JDK is 21, and the foojay resolver that would download the pinned JDK 17 (D02-R1) cannot reach `api.foojay.io` — the egress proxy answers the CONNECT with 403. Every Gradle invocation then fails during configuration with a message about the configuration cache, not about the toolchain.

### DISC-008 — btShapeHull bakes the source shape's margin into its points
`btShapeHull` samples support points from the shape it is handed, and those include that shape's collision margin. Its `buildHull(margin)` argument is ignored. So a source shape carrying a margin yields hull points already pushed one margin outward, and the finished shape adds its own on top — a simplified hull sits two margins outside its mesh, an unsimplified one exactly one. Supersedes the remedy recorded in DISC-004.

### DISC-009 — btShapeHull has no target vertex count; it reduces to 42 and no further
`btShapeHull` cannot be asked for a vertex budget. It samples support points along a fixed set of 42 unit-sphere directions and keeps the unique ones, so its output is bounded at 42 whatever the caller wants. `MAX_HULL_VERTICES` (64) is therefore always satisfied and `MAX_SHARD_HULL_VERTICES` (32) cannot be enforced at runtime at all.

### DISC-010 — Bullet freezes a body's deactivation clock the moment it falls asleep
`btCollisionObject::getDeactivationTime()` accumulates while a body is below the sleeping thresholds and stops the instant the body reaches `ISLAND_SLEEPING`, so it plateaus at Bullet's own `gDeactivationTime` of 2 s and can never reach the 3 s `SLEEP_DESPAWN_S` asks for. `LifetimeSystem` advances it itself for sleeping bodies.

### DISC-011 — Bullet ray tests are locked to DefaultFilter, so a wheel finds no ground
`btDefaultVehicleRaycaster` builds its `ClosestRayResultCallback` internally, on filter group `btBroadphaseProxy::DefaultFilter` (`1<<0`), and offers no way to change it. Bullet's filter test is bidirectional, so a body whose mask omits that bit is invisible to the suspension ray — and D06-S4.4's `STATIC` mask omits it.

### DISC-012 — Bullet reads setBrake as an impulse and applyEngineForce as a force
`btRaycastVehicle::updateFriction` multiplies `m_engineForce` by the timestep and uses `m_brake` directly as a maximum impulse. Passing a force to `setBrake` therefore brakes `1/TICK_DT` — sixty times — too hard.

### DISC-013 — a test resolving LaunchConfig from System.getenv asserts the machine
CI exports `SYNDICATE_STRICT_ASSETS=1` for the whole job. `ServerMain.run` resolved its configuration from `System.getenv()`, so two `ServerMainTest` cases that assert the non-strict asset path passed locally and failed on CI, expecting `OK` and getting `ASSETS_INVALID`.

### DISC-014 — a published braking ordering does not survive a change of mass
The Stampede's published 100-0 is 30 m against the Eclipse's 33 m, and in simulation it stops **longer** — 34.9 m against 33.7 m. A ray-cast wheel's brake is clipped to its own friction circle, so 31% more mass beats 25% more grip and the ordering inverts.

### DISC-015 — a Bullet contact point carries the compound child index in getIndex0/getIndex1
`btManifoldPoint.getIndex0()` and `getIndex1()` are the compound child indices of the two bodies in a contact — the "which part was hit" that D07-S5.1's hit resolution is built on. Nothing in gdx-bullet's API names them that; they are `m_index0`/`m_index1`, set by `btCompoundCollisionAlgorithm` through `setShapeIdentifiersA/B`, and their meaning depends on what kind of shape the body has.

### DISC-016 — a skinned mesh's node transform is not its placement
`GltfReader` composes each node's own transform and ignores joint weights (DEC-035). For ten of the Eclipse's 171 meshes that is wrong by up to **2.65 m**: the whole rear-left corner, which the file places through its joint matrices. Read the way the reader reads it, the car has three wheels and a fourth floating over the front axle.

### DISC-017 — Bullet ray tests a big convex box to about 14 cm
`btCollisionWorld::rayTest` finds a convex shape with an iterative subsimplex cast, not an analytic intersection, and its accuracy degrades with the shape's size. Against the test scene's 120 × 2 × 800 m ground box each suspension ray came back with a contact point up to **0.14 m** above the surface, differently every tick. A `btStaticPlaneShape` at the same place is exact.

### DISC-018 — an object in a downloaded car is a material group, not a part
`syndicate_dissect` treated each imported object as an island. On both shipped cars an object is everything sharing one material, spanning the whole vehicle: one Eclipse object is the entire cabin, another is both headlights and both tail lights at once; the Stampede's `Window_Material1` object is all of its glass. True connected-component separation turns 171 objects into **6,830 shells** and 1,101 into **6,078**, in 16 seconds each.

### DISC-019 — one shipped car names its materials, the other does not
Labelling shells by material name plus a physical glass test covers **99.4% of the Stampede's triangles and 35.9% of the Eclipse's**. The Stampede's materials are called `…Window_Material1` and `…CallipersCalliperA_Zone…`; the Eclipse's are called `bw00.001`, `bwfmp`, `bwfgd` and `oyctp`. Any segmentation design that relies on names works on one of the two cars this project ships.

### DISC-020 — assets/ was not a test input, so content changes never re-ran the tests
`assets/` is on no source set, so Gradle had no reason to believe it affects `test`. Regenerating the shipped meshes left every test task `UP-TO-DATE`, and the suite reported green against the meshes it had seen on some earlier run. CI, which has no such cache, failed six tests on the same commit.

### DISC-021 — a guardrail reading project.rootDir fails only when it finds a violation
`HeadlessSafetyCheckTask` formatted its violation paths with `file.relativeToOrSelf(project.rootDir)`. With the configuration cache on, reading `project` at execution time is a build failure — and that line only runs when the task has a violation to report, so the defect would have surfaced for the first time on the commit that broke the rule the task exists to guard.

### DISC-022 — a same-tick event is consumed before presentation runs, so every hit was silent
`DamageEvent` was published with `emitSameTick`, which `DamageSystem` (12) drains within the tick. PRESENT systems run after the tick, on the deferred bus, so slots 24 and 25 never saw a single hit: cars took damage with no spark and no noise, and the unit tests for both systems passed because they emitted their own events.

### DISC-023 — a crossfade removes a loop's value discontinuity and hides its phase one
`Waveform.loopable(seconds)` trimmed a crossfade measured in *seconds* off a buffer whose length had been chosen to hold a whole number of *cycles*, destroying exactly the property the caller established. Three of the six shipped engine loops — I4, V8 and V10 — held a fractional number of cycles and warbled once per pass. The existing loop test was green on all six, because a crossfade guarantees continuity in value and says nothing about phase.

### DISC-024 — jitpack.io is blocked by the sandbox proxy, so game-client cannot be built here
`game-client` cannot be compiled in the remote agent sandbox: its one JitPack-only dependency, `gdx-gltf` (DEV-001), is denied by the egress proxy with a 403 on CONNECT, and the artifact is not in the Gradle cache. Every other module builds and tests normally. The workaround is to compile the touched package directly with `javac` against the cached jars, which type-checks the code without resolving the unreachable dependency.

### DISC-025 — a parallel band-pass bank with no dry path is a 25 dB gate, not an exhaust
`SoundSynth.resonate` summed three band-pass filters and emitted only their output. Everything between the formants was attenuated by the sum of their skirts *and* by phase cancellation between them. For the 105 / 560 / 1750 Hz set that put a 25 dB hole at 267 Hz — which is a V8's firing frequency at the 4,000 rpm reference. The loudest component of the shipped `engine_loop_v8.wav` was 100 Hz.

### DISC-026 — measured against real engines, the synthesiser was too dark and far too clean
Thirty-nine CC-licensed engine recordings give two numbers to tune against: real exhaust harmonics fall at about **−7 dB per octave** from 100 Hz to 4 kHz, and stand **+9 to +17 dB** above the floor between them. `EngineSynth` measured **−22 dB/oct** and **+37 dB** — muffled, and almost noiseless. The second is what makes a synthesised engine sound synthesised.

### DISC-027 — an engine's periodic parts must all be tied to the crank, or it reads as a fault
The ignition was reported as sounding like "an audio error". Three separate constants in it were free of the engine: the crank-speed modulation ran at a hardcoded 6 Hz, the starter's gear whine held a fixed pitch, and a cranking engine made almost no exhaust noise at all. Tied to the crank, the same code produces a growl that differs per arrangement.

### DISC-028 — a real V8 recording says the even orders were 14 dB light, and why
The user supplied a Ford Mustang GT startup recording. Measured against it at matched engine speed, this synthesiser's odd sub-orders were within 7 dB and its **even** sub-orders 14 dB light. The cause is that two banks which are exact time-reverses of each other cancel their even content, and nothing in the model broke that symmetry. Fixed per-cylinder trims do; random jitter cannot.

### DISC-029 — the ignition timing was wrong in every dimension, against a real start
The same Mustang GT recording carries a complete ignition, and the model's timing was wrong in all three of its parts: the crank was too long, the catch was a ramp where the real one is a step, and the flare collapsed in a single frame instead of holding and decaying.

### DISC-030 — the rumble is an envelope ratio, and the sub-orders were never the problem
An engine's lope is the ratio of sub-order to firing-order *envelope* modulation. A real cross-plane V8 idling measures 17.4 and 18.2 on two steady windows. This synthesiser measured 1.25 — and the reason was not weak sub-orders. Those were already right.

### DISC-031 — fusing exhaust pulses with a comb re-creates the defect it is fixing
Successive blowdowns have to overlap for an engine to rumble rather than pump (DISC-030). The obvious mechanism — delay lines with the reflections mixed back — pins the spectrum, which is exactly the DISC-025 defect this whole rewrite exists to remove. An all-pass chain cannot.

### DISC-032 — a clip of several cars was used as a single-engine reference
A supplied recording of multiple cars starting was measured as though it were one four-cylinder at 1,450 rpm, giving a rumble ratio of 8.4. That number was then chased through a parameter search for most of a session. Its envelope-modulation peaks form no coherent order series at all.

### DISC-033 — a matching sub-order total hid a lope in the wrong place
DISC-030 fixed the ratio of sub-order to firing-order modulation and the user still reported no feelable rumble. They were right. The total was correct and every percentage point of it was at the wrong order.

### DISC-034 — the overrun crackle ran off a clock and sounded like popcorn
`CRACKLE_RATE_HZ = 26.0` produced 11 to 14 evenly spaced bangs a second on every arrangement at every engine speed. It is the same fault as the hardcoded 6 Hz crank in R38a5, introduced two sessions after that one was recorded, by the same hand.

### DISC-035 — `volume × density` is the wrong mass rule for vehicle art, by 25×
D09-R16 makes a part's mass `volume × density`, and G7's mass conservation across a fracture is a consequence of it. Applied to a car's own panels it is not merely imprecise, it is wrong in kind: a door skin either encloses a tenth of a cubic metre of air (785 kg of "steel") or encloses nothing at all (0 kg). Neither number is within an order of magnitude of the 29 kg a door weighs.

### DISC-036 — a bearing of exactly zero lands in the last sector, not the first
`angular_coverage_deg` bins a vertex by `degrees(atan2(dy, dz)) % 360`. A vertex sitting exactly on the axle's `+z` bearing has `dy = 0` — except that the axle centre it is measured against is derived from a bounding box, so `dy` comes out as `-2.8e-17`, the bearing as `-1.8e-14`, and the modulo sends it to **359.99999…**, which is the twenty-fourth sector rather than the first.

### DISC-037 — a material name defined an axle, and took a third of the car with it
The Eclipse carries a flat bracket, 0.35 × 0.10 m and round to 0.29, whose material is called `vehicle_generic_smallspecmap_WHEEL`. C3 matches the whole token `wheel` and labels it one. As a **seed** for a wheel corner it dragged the front axle 0.37 m rearward, inflated the wheel to 1.44 m across, captured 891 shells — 37% of the car — and exported every one of them as brake furniture.

### DISC-038 — real art's topology decides what every later stage can do
Welding doubled vertices more than **halves** the shell count on both shipped cars — the Eclipse goes from 6,830 shells to 2,878 — and dissolving slivers is what makes damage morphs possible at all: with 502 degenerate faces removed the chassis could not be dented, and with 11,988 removed it dents at the second amplitude tried.

### DISC-039 — the fracture tool cannot cut a windscreen, and says so seven different ways
Every `glass` part on both shipped cars failed to fracture. The D09 tool is sound and its self-verification is what caught each failure, but a solidified 5 mm curved windscreen is not the kind of solid its Voronoi path was built and fixture-tested against, and it fails on three different guards depending on the pane.

## Progress

| ID | Title | Date | Status | Related Docs |
|---|---|---|---|---|
| [PROG-001](progress/PROG-001.md) | Build system and ECS core exist; simulation subsystems not started | 2026-08-07 | superseded (by PROG-002) | D00-S4.2, D02-S4.5 |
| [PROG-002](progress/PROG-002.md) | Destruction pipeline works end to end; game systems still not started | 2026-08-08 | superseded (by PROG-003) | D09-S5.1, D14-S5.2 |
| [PROG-003](progress/PROG-003.md) | The first simulation system runs; the Bullet step is wired into the schedule | 2026-08-08 | superseded (by PROG-005) | D06-S5.4, D04-S4.4 |
| [PROG-004](progress/PROG-004.md) | EntityDestroySystem handles deferred destruction and native teardown | 2026-08-08 | superseded (by PROG-005) | D04-S5.5, D04-S4.4 |
| [PROG-005](progress/PROG-005.md) | Vehicles have collision shapes, correct mass properties, and parts that fracture | 2026-08-08 | superseded (by PROG-006) | D06-S5.2, D06-S5.7, D07-S5.6 |
| [PROG-006](progress/PROG-006.md) | All four detach triggers work, and what leaves a vehicle becomes debris | 2026-08-08 | superseded (by PROG-007) | D07-S5.7, D04-S4.4, D06-S5.6 |
| [PROG-007](progress/PROG-007.md) | vehicles are built from assemblies, and what is in the world expires | 2026-08-09 | superseded (by PROG-010) | D04-S4.4, D05-S5.2, D07-S5.8 |
| [PROG-008](progress/PROG-008.md) | the destruction toolchain works end to end and has not been touched since | 2026-08-09 | active | D09-S5.1, D14-S5.2, D13-S4.1 |
| [PROG-009](progress/PROG-009.md) | no shipped content, and both runtime shells are still stubs | 2026-08-09 | superseded (by PROG-011) | D08-S4.6, D03-S5.1, D02-S4.5 |
| [PROG-010](progress/PROG-010.md) | a vehicle can be driven; nine of the 27 systems exist | 2026-08-09 | superseded (by PROG-014) | D04-S4.4, D05-S5.6, D06-S5.5 |
| [PROG-011](progress/PROG-011.md) | the dedicated server runs as a process; there is still nothing to put in it | 2026-08-09 | superseded (by PROG-012) | D03-S5.1, D03-S5.4, D08-S4.6 |
| [PROG-012](progress/PROG-012.md) | two calibrated vehicles ship; only their meshes are missing | 2026-08-09 | active | D08-S4.2, D05-S5.6, D03-S5.1 |
| [PROG-013](progress/PROG-013.md) | meshes can be read; the art is one model per car, not per part | 2026-08-09 | superseded (by PROG-015) | D08-S5.3, D08-S4.1, D03-S5.1 |
| [PROG-014](progress/PROG-014.md) | two vehicles can now hurt each other; 14 of the 27 systems exist | 2026-08-10 | active | D04-S4.4, D07-S5.2, D01-S4.4 |
| [PROG-015](progress/PROG-015.md) | there is a world to fight in, and a gate on the content that fills it | 2026-08-10 | superseded (by PROG-016) | D08-S5.2, D08-S4.7, D03-S5.1 |
| [PROG-016](progress/PROG-016.md) | the art is split; every part has its own mesh and its own collision hull | 2026-08-10 | superseded (by PROG-017) | D08-S4.1, D08-S5.2, D03-S5.1 |
| [PROG-017](progress/PROG-017.md) | an assembled car drives, its wheels turn, and one of them comes off | 2026-08-10 | active | D04-S4.4, D06-S5.5, D14-S5.11 |
| [PROG-018](progress/PROG-018.md) | a match plays itself, headless, from lobby to scoreboard | 2026-08-10 | superseded (by PROG-020) | D04-S4.4, D11-S5.7, D11-S5.8 |
| [PROG-019](progress/PROG-019.md) | a car is segmented by geometry, and the whole game makes a noise | 2026-08-10 | superseded (by PROG-024) | D15-S5.1, D15-S8, D08-S4.3 |
| [PROG-020](progress/PROG-020.md) | the game has a window; 23 of the 27 systems exist | 2026-08-11 | superseded (by PROG-023) | D04-S4.4, D03-S5.1, D03-S5.3 |
| [PROG-021](progress/PROG-021.md) | every sound family plays, and two cars breathe differently | 2026-08-12 | active | D15-S8, D04-S4.4, D06-S5.5 |
| [PROG-022](progress/PROG-022.md) | engines are synthesised, placed, and sound like the cars they came from | 2026-08-12 | active | D15-S8, D02-S4.5 |
| [PROG-023](progress/PROG-023.md) | all 27 systems exist; replication runs end to end over loopback | 2026-08-13 | active | D04-S4.4, D10-S5.4, D10-S5.5 |
| [PROG-024](progress/PROG-024.md) | a model goes in and a vehicle comes out; all nine preparation stages run | 2026-08-13 | active | D15-S5.1, D15-S5.8, D08-S4.2 |

### PROG-001 — Build system and ECS core exist; simulation subsystems not started
The Gradle multi-project build, the guardrail check tasks, `shared-models`, the `game-core` ECS engine, and the `memory-system` tooling are implemented and green. Physics, vehicle, damage, net, AI, and match are not started; the asset schemas and the Blender tool remain the blocking prerequisites for everything downstream of them.

### PROG-002 — Destruction pipeline works end to end; game systems still not started
The asset half of the project is functional: the Blender tool fractures a mesh and verifies its own output, the harness re-verifies that output inside Bullet, and the visual mode renders the destruction and captures it. The game half is still the ECS engine and a component catalogue with no systems on top of it.

### PROG-003 — The first simulation system runs; the Bullet step is wired into the schedule
`game-core` has its first system: `PhysicsSystem` in schedule slot 10, stepping a real `PhysicsWorld` at a fixed `TICK_DT` and mirroring Bullet's state into `Transform` and `Velocity`. The harness now runs on that same world, and all five fixtures verify 31/31 against it. The other 26 systems of D04-S4.4 remain unwritten, and nothing yet spawns a vehicle.

### PROG-004 — EntityDestroySystem handles deferred destruction and native teardown
`EntityDestroySystem` (slot 27, CLEANUP phase) is implemented. It safely tears down entities that were queued for destruction. This includes recursive child entity destruction, releasing native resources like Bullet physics rigid bodies and constraints in the proper dependency order, and returning component instances to their object pools. `World`'s internal destroy queue has been exposed properly so that `EntityDestroySystem` runs within the schedule rather than as a hidden operation.

### PROG-005 — Vehicles have collision shapes, correct mass properties, and parts that fracture
`ShapeCache` (D06-S5.2) owns every collision shape, `MassPropertySystem` (slot 15) keeps mass, COM and inertia true to a vehicle's live parts, and `FractureSystem` (slot 13) turns a destroyed part into its manifest's shards with inherited momentum. Four of D04-S4.4's 27 systems now exist. `DetachSystem` (14) and everything that spawns a vehicle in the game rather than in a test are still unwritten.

### PROG-006 — All four detach triggers work, and what leaves a vehicle becomes debris
`DetachSystem` (slot 14) implements all four detachment triggers of D07-S5.7, wrecks a vehicle whose chassis died, and turns every detached part into a debris body — including the subtree PROG-005 left bodyless below a fractured part. Five of D04-S4.4's 27 systems now exist. Nothing yet spawns a vehicle outside a test, and no hit normal exists for the detach kick.

### PROG-007 — vehicles are built from assemblies, and what is in the world expires
`SpawnSystem` (5) and `LifetimeSystem` (16) exist, and `VehicleFactory` implements D05-S5.2: one entity per part, the slot graph, the recentred compound, the chassis body with its mass properties already correct, and a `btRaycastVehicle` with one ray per wheel. Seven of D04-S4.4's 27 systems now exist. Nothing drives a vehicle yet — slots 6 and 7 are unwritten — and no real content loads, because collision meshes need a glTF reader `game-core` does not have (DEV-010).

### PROG-008 — the destruction toolchain works end to end and has not been touched since
The Blender destruction tool fractures a mesh, verifies its own output and exits with the D09-S4.3 codes; the libGDX + Bullet harness re-verifies that output on the game's own physics world, and its visual mode renders the result. All five D14-S7.1 fixtures pass. This entry exists because that state was last recorded in PROG-002, which a global supersession chain had buried (DEC-023).

### PROG-009 — no shipped content, and both runtime shells are still stubs
`assets/` holds one material table and nothing else; `schemas/` is empty; `asset-pipeline` exits 70; `game-client` and `game-server-headless` are `main` methods that do nothing. The game can now build a vehicle from an assembly, but there is no authored vehicle to build and no process that would run it.

### PROG-010 — a vehicle can be driven; nine of the 27 systems exist
`VehicleStatsSystem` (6) and `VehicleControlSystem` (7) exist, with the degradation model of D05-S5.4 underneath them. A spawned vehicle now aggregates its parts into engine force, brakes, steering and a derived top speed, and drives on scripted input over a ground plane. Nine of D04-S4.4's 27 systems exist. Damage still only happens when a test says so — slots 11 and 12 are what change that.

### PROG-011 — the dedicated server runs as a process; there is still nothing to put in it
`game-server-headless` boots: natives, assets, world, mode-filtered schedule, a 60 Hz tick loop with D03-S5.4's overload resync, and the teardown of D03-S5.6. What it has no way to do is put anything in that world — there is no arena, no authored vehicle, and no transport for a client to arrive on.

### PROG-012 — two calibrated vehicles ship; only their meshes are missing
`assets/` holds two complete vehicles — six part types and two assemblies — whose mass, power, braking and aerodynamics are derived from a published road supercar and a published GT3 racer. They load, validate, spawn and drive to their reference cars' figures in tests. The one thing they cannot do is render, because no `mesh.glb` can be read yet (DEV-010).

### PROG-013 — meshes can be read; the art is one model per car, not per part
`game-core` reads glTF headlessly and the loader uses it, so DEV-010 is closed and a `.glb` in a part directory becomes that part's hull. The two shipped vehicles are renamed Eclipse and Stampede, and the Stampede is recalibrated onto the Mustang GTD the supplied art actually is. What is still missing is the split: the supplied models are one mesh per whole car, wheels included.

### PROG-014 — two vehicles can now hurt each other; 14 of the 27 systems exist
The five combat systems of Phase 5 exist: collisions become damage, damage drives the state machine and propagates, weapons fire, projectiles fly, and kills score. Fourteen of D04-S4.4's 27 systems now exist, and everything the destruction pipeline has been able to do since Phase 1 is finally triggered by the game rather than by a test.

### PROG-015 — there is a world to fight in, and a gate on the content that fills it
An arena ships and loads: `ServerRuntime` now ticks a world with a floor, walls, a kill plane and six spawn points rather than an empty universe. `asset-pipeline` stops exiting 70 and became a real build gate that walks `assets/`, cross-checks it against itself and writes `asset-index.json`. The split — one whole-car model into five parts — is still what stands between the art and a car in the game.

### PROG-016 — the art is split; every part has its own mesh and its own collision hull
The blocker PROG-013 and PROG-015 both named is gone. `syndicate_dissect` cuts each whole-vehicle model into a chassis and four wheels by geometry (DEC-042), and both shipped cars now have six part meshes with `_col` hulls that the game's own reader loads and measures to the figures the source art was independently measured to two sessions ago.

### PROG-017 — an assembled car drives, its wheels turn, and one of them comes off
The shipped content is now a vehicle rather than a set of files that describe one. Both cars settle on their own wheels to the millimetre, their tyres roll through the distance travelled, a wheel destroyed at 30 m/s becomes debris while the car drives on, and there are captures of all of it.

### PROG-018 — a match plays itself, headless, from lobby to scoreboard
Slots 3 and 4 exist, and with them the simulation stops being a sandbox: `MatchSimulatorMain` plays complete matches with no window, eight bots driving four hundred metres each, and writes a report. Seventeen of D04-S4.4's 27 systems now exist, and slot 1 joins them from the client side.

### PROG-019 — a car is segmented by geometry, and the whole game makes a noise
`syndicate_prepare` implements D15-S5.1 stages 1 to 6 and reproduces D15-R16's measurements exactly on both shipped cars. `assets/audio/` holds 52 synthesised sounds covering every event family D15-S8 names. The material table gains an audio axis and the part schema gains a destruction class.

### PROG-020 — the game has a window; 23 of the 27 systems exist
`game-client` boots a real window, builds the same world `ServerRuntime` does, and runs the five presentation slots on top of it. Eight cars drawn from their own art, an arena, a chase camera, a HUD, damage morphs, particle bursts and a 52-sound bank now have a process that uses them. Twenty-three of D04-S4.4's 27 systems exist; the four that remain are all networking.

### PROG-021 — every sound family plays, and two cars breathe differently
The bank goes from 52 sounds to 74 and from six silent-in-practice families to none. Engines are rebuilt as exhaust pulse trains with formants and real firing geometry, forced induction exists as a second voice, and the three families that had correct files and no triggers — tyre, weapon, debris settle — now have them.

### PROG-022 — engines are synthesised, placed, and sound like the cars they came from
Engine audio moves out of the bank and into the runtime. `EngineSynth` renders an engine from its live state a block at a time; `EngineMixer` places up to 24 of them in the world with distance, panning, air absorption and propagation delay; `EngineRunState` maps ignition, shutdown and damage onto synthesiser parameters. The bank goes 74 files to 47, and the two tests that could not fail are replaced by five that can.

### PROG-023 — all 27 systems exist; replication runs end to end over loopback
The four networking slots of D04-S4.4 — 2, 18, 19 and 20 — are implemented, and with them the system catalogue is complete. An authority and a client exchange a handshake, spawns, structural events, bit-packed delta snapshots and validated input across a `Transport`, verified end to end over `LoopbackTransport`. What does not exist yet is a socket transport and the runtime wiring that would use one.

### PROG-024 — a model goes in and a vehicle comes out; all nine preparation stages run
`syndicate_prepare` implements all nine stages of D15-S5.1. Given a directory holding a downloaded model and nothing else, it corrects the model's frame, cleans its topology, separates and labels it, refines those labels into roles a human would name, resolves which pieces turn with each wheel, rigs the panels that open, authors deformation and fracture per destruction class, and writes `assets/parts/<id>/{mesh.glb,part.json}` plus `assets/vehicles/vehicle_<name>_01/assembly.json`.

## Spec Deviations

| ID | Title | Date | Status | Related Docs |
|---|---|---|---|---|
| [DEV-001](spec_deviations/DEV-001.md) | gdx-gltf 3.x does not exist; the pinned version is 2.3.0 from JitPack | 2026-08-08 | active | D02-S4.1, D08-S4.5 |
| [DEV-002](spec_deviations/DEV-002.md) | Cell Fracture is unavailable; cells built by half-space clipping | 2026-08-08 | active | D09-S5.2, D09-S4.1 |
| [DEV-003](spec_deviations/DEV-003.md) | Hull simplification maximises remaining volume, not minimum volume increase | 2026-08-08 | active | D09-S5.5, D14-S6.4 |
| [DEV-004](spec_deviations/DEV-004.md) | test_complex_hollow does not fracture; the fixture is unprocessed | 2026-08-08 | resolved | D14-S7.1, D09-S5.2 |
| [DEV-005](spec_deviations/DEV-005.md) | cells on curved sources are not true Voronoi cells | 2026-08-08 | resolved | D09-S5.2, D14-S7.1 |
| [DEV-006](spec_deviations/DEV-006.md) | test_complex_hollow has no internal cross rib and weighs 2127 kg, not 2500 | 2026-08-08 | active | D14-S7.1, D09-S12 |
| [DEV-007](spec_deviations/DEV-007.md) | TestWorld builds its own Bullet world at 0.005 m, not via PhysicsWorld.create | 2026-08-08 | resolved | D14-S5.5, D06-S5.1 |
| [DEV-008](spec_deviations/DEV-008.md) | btRaycastVehicle has no removeWheel; detach reindexes only the ECS side | 2026-08-08 | superseded (by DEV-015) | D05-S5.5, D06-S5.5 |
| [DEV-009](spec_deviations/DEV-009.md) | a hanging part gets no constraint unless it is already a separate body | 2026-08-08 | active | D06-S5.6, D07-S5.7 |
| [DEV-010](spec_deviations/DEV-010.md) | the asset loader does not read collision meshes; they arrive through a seam | 2026-08-09 | resolved | D08-S5.3, D02-S4.5, D03-S5.5 |
| [DEV-011](spec_deviations/DEV-011.md) | the dedicated server runs its own loop; no libGDX Application is created | 2026-08-09 | active | D03-S5.1, D03-S5.4, D02-S4.5 |
| [DEV-012](spec_deviations/DEV-012.md) | every body is added with DefaultFilter in its mask, beyond D06-S4.4's table | 2026-08-09 | active | D06-S4.4, D06-S5.5 |
| [DEV-013](spec_deviations/DEV-013.md) | art-source holds whole-vehicle glTF in a bucket D08-R1 does not name | 2026-08-09 | active | D08-S4.1, D08-S4.6, D05-S4.1 |
| [DEV-014](spec_deviations/DEV-014.md) | an arena's collision is generated from its bounds, not loaded from collision.glb | 2026-08-10 | active | D08-S4.7, D06-S4.3, D04-S5.4 |
| [DEV-015](spec_deviations/DEV-015.md) | a detached wheel's native slot is disarmed, and indices never renumber | 2026-08-10 | active | D05-S5.5, D06-S5.5 |
| [DEV-016](spec_deviations/DEV-016.md) | the crank is twice as long as the recording it was measured from | 2026-08-13 | active | D15-S8 |
| [DEV-017](spec_deviations/DEV-017.md) | a replay steps the whole physics world, not just the local vehicle's body | 2026-08-13 | active | D10-S5.5, D06-S5.4 |
| [DEV-018](spec_deviations/DEV-018.md) | eight of the catalogue's messages have wire ids and no codec | 2026-08-13 | active | D10-S4.2, D10-S6 |

### DEV-001 — gdx-gltf 3.x does not exist; the pinned version is 2.3.0 from JitPack
D02-S4.1 (D02-R1) named "gdx-gltf 3.x" as the runtime glTF importer. No 3.x release exists; the newest is 2.3.0, and gdx-gltf is published to JitPack only, never to Maven Central. The catalog pins 2.3.0 and D02-S4.1 was amended in the same commit.

### DEV-002 — Cell Fracture is unavailable; cells built by half-space clipping
D09-S5.2 drives Blender's Cell Fracture add-on (`bpy.ops.object.add_fracture_cell_objects`), which wraps voro++. That add-on is not part of Blender 4.2's core distribution — it moved to the extensions platform — and is absent from both a stock install and the `bpy` PyPI module. Cells are constructed directly instead, by clipping a copy of the source mesh with the perpendicular-bisector half-spaces between sites.

### DEV-003 — Hull simplification maximises remaining volume, not minimum volume increase
D09-S5.5 says hull simplification repeatedly drops the vertex whose removal *increases* the hull volume least. That rule is correct for simplifying a concave outline and wrong for a convex hull, where removing any vertex can only shrink it — so as written it selects the single most destructive removal at every step. The implementation maximises the remaining volume instead, then re-inflates to restore enclosure.

### DEV-004 — test_complex_hollow does not fracture; the fixture is unprocessed
**Resolved 2026-08-08 by convex decomposition of the source (DEC-011).** Four of the five D14-S7.1 fixtures process to exit 0. `test_complex_hollow` does not: the shard mass sum comes out at 13571 kg against a true part mass of 2127 kg — 6.4x — so the tool correctly refuses it with `MASS_IMPLAUSIBLE` (72). The fixture is excluded from `:blender-tool:processFixtures` until the cause is fixed. It is now included and processes to exit 0: 14 shards summing to 2127.2 kg, 0.006% from the part mass.

### DEV-005 — cells on curved sources are not true Voronoi cells
**Resolved 2026-08-08 by the exact half-space path.** The bisect-and-fill cell construction of DEV-002 produced exact Voronoi cells on a planar-faced source and materially wrong ones on a curved source: on `test_sphere_r0.5`, 178 of 2400 sampled interior points were closer to a foreign site than to their own cell's, median depth 5.5% of the part's extent. Cells are now built as exact convex polytopes from half-spaces, and all three Voronoi properties hold on every convex fixture including a 100-shard case.

### DEV-006 — test_complex_hollow has no internal cross rib and weighs 2127 kg, not 2500
D14-R20 describes `test_complex_hollow` as a hollow 1 m box with 0.05 m walls **and one internal cross rib**, at ≈ 2500 kg. The checked-in fixture is the shell alone: no rib, 0.2710 m³, 2127.4 kg at steel density. The gap predates this session and was never recorded.

### DEV-007 — TestWorld builds its own Bullet world at 0.005 m, not via PhysicsWorld.create
D14-R10 requires the harness to construct its Bullet world through `game-core`'s `PhysicsWorld.create()`, and D06-R13 fixes the convex collision margin at 0.01 m. `TestWorld` did neither: it built its own world at 0.005 m, on Bullet's default solver settings. **Resolved 2026-08-08** — it delegates to `PhysicsWorld`, and its margin constant is an alias of the game's.

### DEV-008 — btRaycastVehicle has no removeWheel; detach reindexes only the ECS side
D05-S5.5 step 2 calls `vehicleController.removeWheel(index)` when a wheel part detaches. gdx-bullet 1.14.2's `btRaycastVehicle` exposes `addWheel` and no removal of any kind, so `PartDetachment` performs the wheel-index re-densification D05-R24 requires on the ECS side and leaves the native controller untouched.

### DEV-009 — a hanging part gets no constraint unless it is already a separate body
D06-R21 gives a destroyed-but-not-yet-detached part a `btFixedConstraint` so it hangs by a thread and breaks under load. A constraint joins two rigid bodies, and an attached part is not one — it is geometry inside the vehicle's compound (D06-R20, DEC-004). `DetachSystem` therefore creates the constraint only for a part that already has its own body; for every other part the `HANGING_TICKS` delay alone is the hang.

### DEV-010 — the asset loader does not read collision meshes; they arrive through a seam
D08-S5.3's `AssetRegistry.load` calls `GltfLoader.loadNode` to get a part's collision geometry out of `mesh.glb`. `game-core` has no glTF reader and cannot have gdx-gltf's, so `AssetLoader` takes the mesh from a `CollisionMeshSource` supplied by its caller and reports A503 when that returns nothing.

### DEV-011 — the dedicated server runs its own loop; no libGDX Application is created
D03-S5.1 step 4 wraps the headless server in a `HeadlessApplication`. `ServerMain` sets `Gdx.files = new HeadlessFiles()` and runs `HeadlessLoop` on the main thread instead.

### DEV-012 — every body is added with DefaultFilter in its mask, beyond D06-S4.4's table
`PhysicsWorld.addBody(body, layer)` passes `layer.mask() | 1` to Bullet rather than `layer.mask()`. The extra bit is `btBroadphaseProxy::DefaultFilter`, without which a ray-cast wheel finds no ground on any surface (DISC-011).

### DEV-013 — art-source holds whole-vehicle glTF in a bucket D08-R1 does not name
`art-source/` gains a `vehicles/` directory holding two whole-car glTF models — body and wheels in one mesh — rather than the per-part `.blend` files D08-S4.1's tree is made of. They are source art that has not been split into parts yet, and there was nowhere in the specified layout to put them.

### DEV-014 — an arena's collision is generated from its bounds, not loaded from collision.glb
`ArenaFactory` builds an arena's floor and four walls as convex boxes derived from `boundsMin`, `boundsMax` and `groundY`. D08-S4.7 gives an arena an `assets.collision` glTF file; the shipped arena declares none, and nothing reads one.

### DEV-015 — a detached wheel's native slot is disarmed, and indices never renumber
Supersedes DEV-008. `btRaycastVehicle` still has no `removeWheel`, so a detached wheel keeps its slot in the native controller. That slot's stiffness, damping, maximum force, friction, engine force, brake and steering are set to zero, and the surviving wheels **keep** their indices rather than being re-densified as (D05-R24) requires.

### DEV-016 — the crank is twice as long as the recording it was measured from
R38a9 requires ignition timing to be measured rather than invented, and it was: a Ford Mustang GT cranks for about 0.54 s before catching. Reproduced faithfully, the user reported "a complete lack of a startup sound". The crank is now 1.12 s for an eight — roughly double the measured value — and the starter is louder and audibly labours on each compression.

### DEV-017 — a replay steps the whole physics world, not just the local vehicle's body
D10-S5.5 step 3 replays unacknowledged inputs with `world.stepPhysicsOnly(TICK_DT)` and the comment "only the local vehicle's body needs re-simulating". `ReconciliationSystem` steps the entire `btDiscreteDynamicsWorld` per replayed input and then restores every remote entity from the newest snapshot.

### DEV-018 — eight of the catalogue's messages have wire ids and no codec
`MessageType` holds all twenty-one messages of D10-S4.2 with their wire ids and channels reserved. `Messages` encodes thirteen of them. `ScoreUpdate`, `MatchPhase`, `ChatMessage`, `AdminCommand`, `Ping`, `Pong` and `SelectVehicle` are declared and not yet written or read.

## Session Summaries

| ID | Title | Date | Status | Related Docs |
|---|---|---|---|---|
| [SESS-001](session_summaries/SESS-001.md) | Authored the complete 15-document blueprint suite and operational files | 2026-08-07 | active | D00-S4.2, D13-S4.1 |
| [SESS-002](session_summaries/SESS-002.md) | Scaffolded the Gradle build, guardrail checks, ECS core, and memory tooling | 2026-08-08 | active | D02-S4.4, D04-S4.5, D13-S5.5 |
| [SESS-003](session_summaries/SESS-003.md) | Component catalogue, destruction tool, and verification harness | 2026-08-08 | active | D04-S4.3, D09-S5.1, D14-S5.2 |
| [SESS-004](session_summaries/SESS-004.md) | Exact Voronoi fracture at 100+ shards, and a CI pipeline that runs once | 2026-08-08 | active | D09-S5.3, D12-S5.4, D14-S5 |
| [SESS-005](session_summaries/SESS-005.md) | Convex decomposition of the source; the fifth fixture fractures | 2026-08-08 | active | D09-S5.2, D14-S7.1 |
| [SESS-006](session_summaries/SESS-006.md) | PhysicsSystem — the Bullet step joins the schedule | 2026-08-08 | active | D06-S5.4, D04-S4.4 |
| [SESS-007](session_summaries/SESS-007.md) | the harness runs on the game's physics world | 2026-08-08 | active | D14-S5.5, D06-S4.1, D02-S4.5 |
| [SESS-008](session_summaries/SESS-008.md) | Implement EntityDestroySystem for deferred destruction | 2026-08-08 | active | D04-S5.5 |
| [SESS-009](session_summaries/SESS-009.md) | Shape cache, mass properties, and parts that break apart | 2026-08-08 | active | D06-S5.2, D06-S5.7, D07-S5.6 |
| [SESS-010](session_summaries/SESS-010.md) | DetachSystem — parts leave, and what leaves lands | 2026-08-08 | active | D07-S5.7, D04-S4.4, D06-S5.6 |
| [SESS-011](session_summaries/SESS-011.md) | assemblies become vehicles, and progress entries get one home each | 2026-08-09 | active | D05-S5.2, D07-S5.8, D13-S4.1 |
| [SESS-012](session_summaries/SESS-012.md) | a vehicle you can drive, and a server that runs it | 2026-08-09 | active | D05-S5.6, D06-S5.5, D03-S5.4 |
| [SESS-013](session_summaries/SESS-013.md) | two vehicles derived from real cars, and the roster that tracks them | 2026-08-09 | active | D05-S5.6, D08-S4.2, D06-S5.5 |
| [SESS-014](session_summaries/SESS-014.md) | a glTF reader, two cars on screen, and the vehicles renamed | 2026-08-09 | active | D08-S4.5, D14-S5.3, D05-S5.6 |
| [SESS-015](session_summaries/SESS-015.md) | combat, and a world to have it in | 2026-08-10 | active | D07-S5.2, D04-S4.4, D08-S5.2 |
| [SESS-016](session_summaries/SESS-016.md) | Blender runs here after all, and the art is split | 2026-08-10 | active | D08-S4.1, D02-S4.1, D14-S5.3 |
| [SESS-017](session_summaries/SESS-017.md) | the car drives, the wheels turn, and one comes off | 2026-08-10 | active | D04-S4.4, D06-S5.5, D14-S5.11 |
| [SESS-018](session_summaries/SESS-018.md) | the caliper stops spinning, and segmentation gets measured | 2026-08-10 | active | D15-S1, D15-S5.4 |
| [SESS-019](session_summaries/SESS-019.md) | a match that plays itself, a car cut up by geometry, and a noise | 2026-08-10 | active | D11-S5.7, D15-S5.1, D15-S8 |
| [SESS-020](session_summaries/SESS-020.md) | a window, and the first time anybody can watch it | 2026-08-11 | active | D03-S5.1, D04-S4.4, D07-S5.5 |
| [SESS-021](session_summaries/SESS-021.md) | the cars finally sound like the cars they came from | 2026-08-12 | active | D15-S8, D04-S4.4, D06-S5.5 |
| [SESS-022](session_summaries/SESS-022.md) | the engine stops being a file | 2026-08-12 | active | D15-S8, D02-S4.5 |
| [SESS-023](session_summaries/SESS-023.md) | the engine stops pumping | 2026-08-13 | active | D15-S8 |
| [SESS-024](session_summaries/SESS-024.md) | three complaints, three mechanisms that were not there | 2026-08-13 | active | D15-S8 |
| [SESS-025](session_summaries/SESS-025.md) | the last four systems, and the catalogue is closed | 2026-08-13 | active | D10-S5.4, D04-S4.4 |
| [SESS-026](session_summaries/SESS-026.md) | the preparation pipeline finishes — a model in, a vehicle out | 2026-08-13 | active | D15-S5.1, D15-S5.8, D08-S4.2 |

### SESS-001 — Authored the complete 15-document blueprint suite and operational files
Created the full Syndicate blueprint suite: 15 contractual specification documents in `docs/`, the root operational files `CLAUDE.md` and `JULES.md`, and this `.agent-memory/` structure. No implementation code was written; the repository now holds the contract that future sessions implement against.

### SESS-002 — Scaffolded the Gradle build, guardrail checks, ECS core, and memory tooling
Took the repository from documentation-only to a buildable baseline: an eight-module Gradle build with the pinned toolchain of D02-S4.1, six guardrail check tasks, `shared-models`, the `game-core` ECS engine, and working `.agent-memory` tooling. `./gradlew build check` passes with 65 unit tests green.

### SESS-003 — Component catalogue, destruction tool, and verification harness
Took the repository from "does not compile" to a working destruction pipeline: a mesh goes in, comes out fractured and verified by two independent implementations, and is rendered mid-explosion from the same simulation the checks measured. Cube and sphere captures are in `build/captures/`.

### SESS-004 — Exact Voronoi fracture at 100+ shards, and a CI pipeline that runs once
Replaced the approximate Voronoi cell construction with exact half-space intersection on convex sources, raised the fixture shard counts to 100-200 and captured real mid-explosion renders at that density, then found and fixed the reason a clean clone would not build (DISC-005) and restructured CI so one commit produces one run instead of two.

### SESS-005 — Convex decomposition of the source; the fifth fixture fractures
Resolved DEV-004 by decomposing a non-convex source into disjoint convex pieces, so it reaches the same exact half-space fracture path a convex source does. `test_complex_hollow` went from `MASS_IMPLAUSIBLE` at 6.4x its part mass to exit 0 at 0.006%, and is back in `fixtureSpecs` and in the Voronoi property tests.

### SESS-006 — PhysicsSystem — the Bullet step joins the schedule
Implemented the first of D04-S4.4's 27 systems: `PhysicsSystem` in slot 10, on a new `PhysicsWorld` built to D06-S4.1. The simulation now advances — a body falls, lands, and its `Transform` and `Velocity` components say so.

### SESS-007 — the harness runs on the game's physics world
Resolved DEV-007: `TestWorld` delegates to `PhysicsWorld.create()` instead of building its own Bullet world at a different collision margin. Installing Blender then let the fixtures actually run, and the margin change exposed a real hull-construction bug (DISC-008) that would have shipped into the game.

### SESS-008 — Implement EntityDestroySystem for deferred destruction
Implemented `EntityDestroySystem` to execute the ECS CLEANUP phase (slot 27). This system safely processes `World`'s destroy queue, recursively tears down vehicle components and constraints, and ensures stable disposal of C++ Bullet native resources. Tested and verified logic under D04 constraints.

### SESS-009 — Shape cache, mass properties, and parts that break apart
Implemented `ShapeCache` (D06-S5.2), `MassPropertySystem` (slot 15) and `FractureSystem` (slot 13), plus the pieces they could not exist without. A destroyed armour plate now leaves its vehicle, breaks into its manifest's shards with the momentum it had at its own position, and the vehicle's mass, centre of mass and inertia are correct in the same tick.

### SESS-010 — DetachSystem — parts leave, and what leaves lands
Implemented `DetachSystem` (slot 14): all four detachment triggers, `wreckVehicle`, and a debris body for every part that leaves a vehicle in one piece. The subtree that used to be stranded bodyless below a fractured part now lands with the rest of it.

### SESS-011 — assemblies become vehicles, and progress entries get one home each
Implemented the three things PROG-006 named — `LifetimeSystem` (16), the assembly loader, and `SpawnSystem` (5) with D05-S5.2's vehicle instantiation — then restructured `progress/` after finding that a working subsystem had been buried by a global supersession chain, and added `ROADMAP.md`.

### SESS-012 — a vehicle you can drive, and a server that runs it
Implemented Phase 4: the degradation model, `VehicleStatsSystem` (6) and `VehicleControlSystem` (7), the D03-S5.2 schedule factory, and a real tick loop in `game-server-headless`. A vehicle now drives. Two bugs came out of it — a collision filter that made every suspension ray miss the ground, and wheel tuning constants that had been wrong against D06-S4.5 since they were written.

### SESS-013 — two vehicles derived from real cars, and the roster that tracks them
Added vehicle profiles and shipped two: a road supercar derived from a Maserati MC20 and a GT racer derived from a Ford Mustang GT3, each calibrated from published figures and held to them by tests. Extended the part schema with a `handling` block, added an engine-power stat so a vehicle has a real top speed, and found two bugs in the brake path.

### SESS-014 — a glTF reader, two cars on screen, and the vehicles renamed
Built the headless glTF reader DEV-010 had been waiting for, wired it into the asset loader and the dedicated server, organised the two supplied car models into `art-source/` with verified import corrections, added a source-art check-and-render mode to the harness, and renamed the shipped vehicles to Eclipse and Stampede — recalibrating the Stampede onto the Mustang GTD the art actually is.

### SESS-015 — combat, and a world to have it in
Implemented Phase 5 in full — the five systems that turn a contact or a trigger pull into a part coming off a vehicle — and the parts of Phase 6 that do not depend on the Blender split: an arena that ships and loads, and an asset pipeline that stops being a stub. The system catalogue goes from 9 of 27 to 14 of 27.

### SESS-016 — Blender runs here after all, and the art is split
Built `syndicate_dissect`, a Blender tool that cuts a whole-vehicle model into a chassis and four wheels by geometry rather than by name, and ran it on both shipped cars. The blocker three consecutive progress entries called "a Blender job nobody has run" is closed, and finding it uncovered a placement bug in the shipped glTF reader.

### SESS-017 — the car drives, the wheels turn, and one comes off
Put the wheels on the ground, made them spin, made one detach at speed, and photographed all three. `TransformSystem` fills slot 21 (15 of 27), and `syndicate-verify --vehicle` renders an assembled car from component state.

### SESS-018 — the caliper stops spinning, and segmentation gets measured
Fixed the brake caliper rotating with the tyre, by a physical test rather than a naming one, and measured what a general vehicle segmentation tool could and could not do on the two shipped cars. The answers became `docs/15_vehicle_preparation_pipeline.md`, a new blueprint (D15).

### SESS-019 — a match that plays itself, a car cut up by geometry, and a noise
Phase 8 landed — slots 3 and 4, and a headless match runner that plays a whole match with no display. Then the D15 preparation pipeline, the shared material decision, and a synthesised sound bank in which different cars sound different.

### SESS-020 — a window, and the first time anybody can watch it
Implemented Phase 7: the five presentation slots (22–26), a chase camera, a HUD, the client startup sequence and the fixed-timestep client loop. The client draws eight cars from their own art in a real arena with a live scoreboard, and a capture mode proves it on a machine with no display.

### SESS-021 — the cars finally sound like the cars they came from
Rebuilt the engine synthesiser around exhaust pulse trains and formants, added forced induction as a second voice, added start / stop / overrun and a fire loop, and wired the three families that shipped with correct files and no triggers. The bank goes 52 → 74. Found and fixed a loop-seam bug that had been silently breaking half the engine loops.

### SESS-022 — the engine stops being a file
The user did not believe the engine loops. They were right: the V8's loudest component was 100 Hz against a firing frequency of 266.7 Hz, and four of six loops sounded at a pitch unrelated to their engine. Engine audio is now synthesised at runtime, placed by our own mixer, and tuned against real recordings including a Mustang GT the user supplied. The bank goes 74 files to 47.

### SESS-023 — the engine stops pumping
The user asked for the distinct rumble to be synthesised correctly. It now is, to within a factor of two of a real cross-plane V8, and the reason it was missing turned out not to be the one two sessions had assumed. The sub-orders were already right; the engine simply pumped twelve times too hard once per cylinder.

### SESS-024 — three complaints, three mechanisms that were not there
The user reported no startup sound, no feelable rumble, and a popping "like popcorn being made" on every engine. All three were real, all three were mechanisms the model did not have rather than constants set wrong, and one of them was a fault this project had already recorded and repeated.

### SESS-025 — the last four systems, and the catalogue is closed
Implemented the replication layer: slots 2, 18, 19 and 20, the transport abstraction they sit on, the bit-packed snapshot format, delta compression with per-peer baselines, the jitter buffer, input validation and the handshake. Twenty-seven of D04-S4.4's twenty-seven systems now exist.

### SESS-026 — the preparation pipeline finishes — a model in, a vehicle out
Implemented D15-S5.1's stages 2 (applied rather than reported), 6, 7 and 8, and completed stage 4 with roles and the wheel/hub symmetry pass. `syndicate-prepare` now takes a directory holding a downloaded model and writes a directory of parts and an assembly the game loads.

## Blueprint Coverage

| Doc | Entries referencing it |
|---|---|
| docs/00_master_index.md | DEC-001, DEC-009, DEC-019, DEC-020, DEC-036, PROG-001, SESS-001 |
| docs/01_product_game_design.md | DEC-033, DEC-037, DEC-039, DEC-040, PROG-014 |
| docs/02_technical_architecture.md | DEC-006, DEC-007, DEC-008, DEC-010, DEC-012, DEC-013, DEC-015, DEC-030, DEC-035, DEC-041, DEC-055, DEV-001, DEV-010, DEV-011, DISC-001, DISC-005, DISC-007, DISC-016, DISC-021, DISC-024, PROG-001, PROG-009, PROG-022, SESS-002, SESS-007, SESS-016, SESS-022 |
| docs/03_runtime_modes.md | DEC-007, DEC-030, DEC-048, DEC-049, DEC-050, DEC-051, DEV-010, DEV-011, DISC-013, PROG-009, PROG-011, PROG-012, PROG-013, PROG-015, PROG-016, PROG-020, SESS-012, SESS-020 |
| docs/04_entity_component_model.md | DEC-001, DEC-007, DEC-012, DEC-014, DEC-016, DEC-017, DEC-018, DEC-021, DEC-025, DEC-026, DEC-030, DEC-038, DEC-040, DEC-048, DEC-049, DEC-050, DEC-051, DEC-056, DEC-059, DEC-061, DEC-062, DEV-014, DISC-022, PROG-003, PROG-004, PROG-006, PROG-007, PROG-010, PROG-014, PROG-017, PROG-018, PROG-020, PROG-021, PROG-023, SESS-002, SESS-003, SESS-006, SESS-008, SESS-010, SESS-015, SESS-017, SESS-020, SESS-021, SESS-025 |
| docs/05_vehicle_part_system.md | DEC-002, DEC-004, DEC-014, DEC-016, DEC-017, DEC-018, DEC-019, DEC-021, DEC-022, DEC-024, DEC-025, DEC-027, DEC-028, DEC-029, DEC-031, DEC-032, DEC-033, DEC-034, DEC-037, DEC-039, DEC-043, DEC-047, DEC-052, DEC-053, DEC-054, DEC-063, DEC-064, DEV-008, DEV-013, DEV-015, DISC-014, PROG-007, PROG-010, PROG-012, SESS-011, SESS-012, SESS-013, SESS-014 |
| docs/06_physics_simulation.md | DEC-002, DEC-004, DEC-012, DEC-014, DEC-015, DEC-016, DEC-020, DEC-021, DEC-022, DEC-026, DEC-028, DEC-029, DEC-031, DEC-032, DEC-034, DEC-043, DEC-044, DEC-061, DEC-066, DEV-007, DEV-008, DEV-009, DEV-012, DEV-014, DEV-015, DEV-017, DISC-004, DISC-008, DISC-009, DISC-010, DISC-011, DISC-012, DISC-014, DISC-015, DISC-017, PROG-003, PROG-005, PROG-006, PROG-010, PROG-017, PROG-021, SESS-006, SESS-007, SESS-009, SESS-010, SESS-012, SESS-013, SESS-017, SESS-021 |
| docs/07_damage_destruction_model.md | DEC-005, DEC-017, DEC-018, DEC-038, DEC-049, DEC-051, DEV-009, DISC-010, DISC-015, DISC-022, PROG-005, PROG-006, PROG-007, PROG-014, SESS-009, SESS-010, SESS-011, SESS-015, SESS-020 |
| docs/08_asset_pipeline.md | DEC-003, DEC-019, DEC-020, DEC-022, DEC-024, DEC-027, DEC-031, DEC-033, DEC-035, DEC-036, DEC-037, DEC-039, DEC-041, DEC-042, DEC-043, DEC-045, DEC-046, DEC-063, DEC-064, DEC-065, DEV-001, DEV-010, DEV-013, DEV-014, DISC-016, DISC-018, DISC-020, PROG-009, PROG-011, PROG-012, PROG-013, PROG-015, PROG-016, PROG-019, PROG-024, SESS-013, SESS-014, SESS-015, SESS-016, SESS-026 |
| docs/09_blender_destruction_tool.md | DEC-003, DEC-009, DEC-011, DEC-042, DEC-067, DEV-002, DEV-003, DEV-004, DEV-005, DEV-006, DISC-002, DISC-003, DISC-006, DISC-009, DISC-035, DISC-039, PROG-002, PROG-008, SESS-003, SESS-004, SESS-005 |
| docs/10_networking_multiplayer.md | DEC-005, DEC-050, DEC-058, DEC-059, DEC-060, DEC-061, DEC-062, DEV-017, DEV-018, PROG-023, SESS-025 |
| docs/11_ai_bots_and_match_simulation.md | PROG-018, SESS-019 |
| docs/12_testing_validation_ci.md | DEC-006, DEC-010, DISC-001, DISC-005, DISC-007, DISC-013, DISC-020, DISC-021, DISC-023, DISC-024, DISC-025, SESS-004 |
| docs/13_persistent_memory_system.md | DEC-023, PROG-008, SESS-001, SESS-002, SESS-011 |
| docs/14_test_environment.md | DEC-008, DEC-011, DEC-013, DEC-035, DEC-036, DEV-003, DEV-004, DEV-005, DEV-006, DEV-007, DISC-004, DISC-008, DISC-016, PROG-002, PROG-008, PROG-017, SESS-003, SESS-004, SESS-005, SESS-007, SESS-014, SESS-016, SESS-017 |
| docs/15_vehicle_preparation_pipeline.md | DEC-044, DEC-045, DEC-046, DEC-047, DEC-052, DEC-053, DEC-054, DEC-055, DEC-056, DEC-057, DEC-063, DEC-064, DEC-065, DEC-066, DEC-067, DEV-016, DISC-018, DISC-019, DISC-023, DISC-025, DISC-026, DISC-027, DISC-028, DISC-029, DISC-030, DISC-031, DISC-032, DISC-033, DISC-034, DISC-035, DISC-036, DISC-037, DISC-038, DISC-039, PROG-019, PROG-021, PROG-022, PROG-024, SESS-018, SESS-019, SESS-021, SESS-022, SESS-023, SESS-024, SESS-026 |
