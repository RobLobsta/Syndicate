# Agent Memory Index

**Generated:** 2026-08-08
**Entries:** 37 (active: 32, superseded: 2, resolved: 3)

> This file is generated by `memory-system`. Do not edit by hand.
> Entry format and triggers: docs/13_persistent_memory_system.md#D13-S4.2

## Decisions

| ID | Title | Date | Status | Related Docs |
|---|---|---|---|---|
| [DEC-001](decisions/DEC-001.md) | Own an Ashley-style component model rather than depending on Ashley or Artemis | 2026-08-07 | active | D04-S4.1, D00-S5.2 |
| [DEC-002](decisions/DEC-002.md) | Chose the ray-cast vehicle model over rigid-body wheels | 2026-08-07 | active | D06-S5.5, D05-S5.5 |
| [DEC-003](decisions/DEC-003.md) | glTF 2.0 (.glb) is the runtime mesh format, chosen for morph target support | 2026-08-07 | active | D08-S4.5, D09-S5.6 |
| [DEC-004](decisions/DEC-004.md) | A vehicle is one rigid body with a compound shape, not one body per part | 2026-08-07 | active | D05-S4.1, D06-S5.3 |
| [DEC-005](decisions/DEC-005.md) | Shard trajectories are cosmetic and are never replicated | 2026-08-07 | active | D07-S4.2, D10-S4.3 |
| [DEC-006](decisions/DEC-006.md) | Blueprint invariants are enforced by Gradle check tasks, not by convention | 2026-08-08 | active | D02-S5.6, D12-S5.4 |
| [DEC-007](decisions/DEC-007.md) | ECS interface is EntitySystem; LaunchConfig belongs to shared-models | 2026-08-08 | active | D04-S4.5, D03-S4.2, D02-S4.5 |
| [DEC-008](decisions/DEC-008.md) | The harness parses GLB itself rather than importing it through gdx-gltf | 2026-08-08 | active | D14-S5.13, D02-S4.5 |
| [DEC-009](decisions/DEC-009.md) | MAX_SHARDS_PER_PART raised from 64 to 256 | 2026-08-08 | active | D00-S6.4, D09-S4.2 |
| [DEC-010](decisions/DEC-010.md) | CI runs the D12-S5.4 stages as steps in one job, not one job per stage | 2026-08-08 | active | D12-S5.4, D02-S4.6 |
| [DEC-011](decisions/DEC-011.md) | A non-convex source is decomposed by solid BSP, exactly, not by V-HACD | 2026-08-08 | active | D09-S5.2, D14-S7.1 |
| [DEC-012](decisions/DEC-012.md) | PhysicsWorld is injected into PhysicsSystem, and cross-tick impulses queue on it | 2026-08-08 | active | D06-S5.4, D04-S4.5, D02-S5.7 |
| [DEC-013](decisions/DEC-013.md) | test-environment does not depend on gdx-gltf; D02-S4.5 amended to match | 2026-08-08 | active | D02-S4.5, D14-S5.13 |

### DEC-001 — Own an Ashley-style component model rather than depending on Ashley or Artemis
The simulation uses an in-house, component-based data-oriented model with a fixed explicit system schedule, rather than adopting Ashley (libGDX's ECS) or Artemis-odb.

### DEC-002 — Chose the ray-cast vehicle model over rigid-body wheels
Vehicles use `btRaycastVehicle` — one rigid body plus N ray-cast wheels — rather than rigid-body wheels joined by `btHinge2Constraint`.

### DEC-003 — glTF 2.0 (.glb) is the runtime mesh format, chosen for morph target support
Processed meshes ship as glTF 2.0 binary (`.glb`), imported with gdx-gltf, because it is the only well-supported format that carries morph targets as a first-class feature.

### DEC-004 — A vehicle is one rigid body with a compound shape, not one body per part
Each vehicle is a single `btRigidBody` whose collision shape is a `btCompoundShape` of per-part convex hulls. Parts become independent bodies only at the moment they detach.

### DEC-005 — Shard trajectories are cosmetic and are never replicated
The authority replicates the *fact* of a fracture (part, tick, parent velocity) but never individual shard transforms or velocities. Each client spawns its own shard set locally.

### DEC-006 — Blueprint invariants are enforced by Gradle check tasks, not by convention
Every mechanically checkable rule in D02 and D00 is implemented as a Gradle task in `build-logic/` and wired into `check`, rather than left as a documented convention. The module catalogue of D02-S4.5 is duplicated as data in `ModuleRules.kt` so the checks have something to compare against.

### DEC-007 — ECS interface is EntitySystem; LaunchConfig belongs to shared-models
Two placements the blueprints left open: the ECS system interface is `EntitySystem`, not `System`, and `LaunchConfig` plus its resolver live in `shared-models` (`dev.syndicate.model.config`) rather than in `game-core`.

### DEC-008 — The harness parses GLB itself rather than importing it through gdx-gltf
`test-environment` reads vertex positions and indices out of `.glb` with its own reader (`GlbReader`, ~200 lines) instead of loading the file through gdx-gltf. The decisive reason is that gdx-gltf builds libGDX `Mesh` objects, which are GPU buffers requiring a GL context — and D14-S5.13 requires the headless runner to create none.

### DEC-009 — MAX_SHARDS_PER_PART raised from 64 to 256
The hard cap on shards per part goes from 64 to 256. The old value was chosen before there was a fracture implementation to measure; the exact half-space path makes a 100-shard fracture cost 2 seconds and hold every correctness property, so the cap was bounding nothing real.

### DEC-010 — CI runs the D12-S5.4 stages as steps in one job, not one job per stage
The workflow ran three jobs (stages 0, 1-2, 7) and triggered on both `push: ["**"]` and `pull_request`, so every commit on a branch with an open PR ran the whole pipeline twice: 6 jobs, 573 s of runner time, for one commit's worth of information. It is now a single job whose steps are the stages, triggered on `pull_request` plus `push` to `main` only.

### DEC-011 — A non-convex source is decomposed by solid BSP, exactly, not by V-HACD
A non-convex source is split into disjoint convex pieces by a solid binary space partition over its own face planes, so `cell ∩ source` becomes a union of exact polytope intersections. An approximate decomposition (V-HACD) and a per-cell mesh boolean were both rejected: each gives up the exactness that is the point of the convex path.

### DEC-012 — PhysicsWorld is injected into PhysicsSystem, and cross-tick impulses queue on it
`PhysicsWorld` is a constructor dependency of `PhysicsSystem` rather than a field on `World`, and the "pending impulses" of D06-S5.4 step 1 are a queue owned by `PhysicsWorld`, drained and applied by `PhysicsSystem` in ascending entity id order.

### DEC-013 — test-environment does not depend on gdx-gltf; D02-S4.5 amended to match
`gdx-gltf` is removed from `test-environment`'s dependencies and moved to its "MUST NOT depend on" column in D02-S4.5. No source in the repository ever imported it — DEC-008 made it unused — and it was the one artifact keeping the module from building without JitPack.

## Discoveries

| ID | Title | Date | Status | Related Docs |
|---|---|---|---|---|
| [DISC-001](discoveries/DISC-001.md) | Configuration cache rejects onlyIf lambdas reading a script-level val | 2026-08-08 | active | D02-S4.6, D12-S5.4 |
| [DISC-002](discoveries/DISC-002.md) | Blender writes to stdout at the C level, breaking the tool's JSON-only contract | 2026-08-08 | active | D09-S4.1 |
| [DISC-003](discoveries/DISC-003.md) | bpy module teardown segfaults, overwriting the tool's exit code | 2026-08-08 | active | D09-S4.3 |
| [DISC-004](discoveries/DISC-004.md) | btShapeHull.buildHull(margin) double-counts the collision margin | 2026-08-08 | active | D06-S4.3, D14-S4.5.2 |
| [DISC-005](discoveries/DISC-005.md) | an unanchored `build/` in .gitignore silently untracked build-logic | 2026-08-08 | active | D02-S4.4, D12-S5.4 |
| [DISC-006](discoveries/DISC-006.md) | the point-in-mesh ray test double-counts a ray grazing a shared edge | 2026-08-08 | active | D09-S5.2 |
| [DISC-007](discoveries/DISC-007.md) | the sandbox cannot provision a JDK 17 toolchain; api.foojay.io is blocked | 2026-08-08 | active | D02-S4.1, D12-S5.4 |

### DISC-001 — Configuration cache rejects onlyIf lambdas reading a script-level val
With `org.gradle.configuration-cache=true`, an `onlyIf { }` spec in a `.gradle.kts` file fails to serialise if its body reads a top-level `val` or an unqualified `logger`. The fix is to copy the value into a local inside the task's configure block; the diagnostic never says this.

### DISC-002 — Blender writes to stdout at the C level, breaking the tool's JSON-only contract
D09-R2 requires the tool's stdout to carry exactly one JSON document and nothing else. Blender does not cooperate: its glTF exporter writes a Draco availability notice to the process's stdout at the C level on every run, landing in the middle of the document an agent is about to parse. The tool now duplicates the real stdout to a private descriptor and points fd 1 at stderr.

### DISC-003 — bpy module teardown segfaults, overwriting the tool's exit code
When the tool runs on the `bpy` PyPI module (DEV-002), normal interpreter shutdown after a run that loaded and freed meshes segfaults inside Blender's own teardown. The process then exits 139 regardless of what the tool decided, destroying the exit-code contract D09-S4.3 exists to provide. The entry point calls `os._exit` after flushing instead.

### DISC-004 — btShapeHull.buildHull(margin) double-counts the collision margin
Passing a shape's collision margin to `btShapeHull.buildHull(margin)` offsets the generated hull points outward by that margin, and the `btConvexHullShape` built from the result then adds its own margin on top. A simplified hull therefore has its collision surface two margins outside its mesh while an unsimplified one has exactly one. Pass `0`.

### DISC-005 — an unanchored `build/` in .gitignore silently untracked build-logic
`.gitignore` carried a bare `build/`. Git matches an unanchored pattern against a directory of that name at **any depth**, and `build-logic`'s guardrail tasks live in package `dev.syndicate.build` — so `build-logic/src/main/kotlin/dev/syndicate/build/` was ignored and all seven task classes were never committed. The working tree built perfectly; a clean clone failed to configure with `Unresolved reference: dev`.

### DISC-006 — the point-in-mesh ray test double-counts a ray grazing a shared edge
`fracture._inside_predicate` counts a `+X` ray crossing by triangle parity, and a ray passing exactly along the edge two triangles share is counted **twice** — so the point reads as outside when it is inside. Its docstring claims a tie-break resolves this; there is none.

### DISC-007 — the sandbox cannot provision a JDK 17 toolchain; api.foojay.io is blocked
In the remote agent sandbox the only JDK is 21, and the foojay resolver that would download the pinned JDK 17 (D02-R1) cannot reach `api.foojay.io` — the egress proxy answers the CONNECT with 403. Every Gradle invocation then fails during configuration with a message about the configuration cache, not about the toolchain.

## Progress

| ID | Title | Date | Status | Related Docs |
|---|---|---|---|---|
| [PROG-001](progress/PROG-001.md) | Build system and ECS core exist; simulation subsystems not started | 2026-08-07 | superseded (by PROG-002) | D00-S4.2, D02-S4.5 |
| [PROG-002](progress/PROG-002.md) | Destruction pipeline works end to end; game systems still not started | 2026-08-08 | superseded (by PROG-003) | D09-S5.1, D14-S5.2 |
| [PROG-003](progress/PROG-003.md) | The first simulation system runs; the Bullet step is wired into the schedule | 2026-08-08 | active | D06-S5.4, D04-S4.4 |

### PROG-001 — Build system and ECS core exist; simulation subsystems not started
The Gradle multi-project build, the guardrail check tasks, `shared-models`, the `game-core` ECS engine, and the `memory-system` tooling are implemented and green. Physics, vehicle, damage, net, AI, and match are not started; the asset schemas and the Blender tool remain the blocking prerequisites for everything downstream of them.

### PROG-002 — Destruction pipeline works end to end; game systems still not started
The asset half of the project is functional: the Blender tool fractures a mesh and verifies its own output, the harness re-verifies that output inside Bullet, and the visual mode renders the destruction and captures it. The game half is still the ECS engine and a component catalogue with no systems on top of it.

### PROG-003 — The first simulation system runs; the Bullet step is wired into the schedule
`game-core` has its first system: `PhysicsSystem` in schedule slot 10, stepping a real `PhysicsWorld` at a fixed `TICK_DT` and mirroring Bullet's state into `Transform` and `Velocity`. The other 26 systems of D04-S4.4 remain unwritten, and nothing yet spawns a vehicle.

## Spec Deviations

| ID | Title | Date | Status | Related Docs |
|---|---|---|---|---|
| [DEV-001](spec_deviations/DEV-001.md) | gdx-gltf 3.x does not exist; the pinned version is 2.3.0 from JitPack | 2026-08-08 | active | D02-S4.1, D08-S4.5 |
| [DEV-002](spec_deviations/DEV-002.md) | Cell Fracture is unavailable; cells built by half-space clipping | 2026-08-08 | active | D09-S5.2, D09-S4.1 |
| [DEV-003](spec_deviations/DEV-003.md) | Hull simplification maximises remaining volume, not minimum volume increase | 2026-08-08 | active | D09-S5.5, D14-S6.4 |
| [DEV-004](spec_deviations/DEV-004.md) | test_complex_hollow does not fracture; the fixture is unprocessed | 2026-08-08 | resolved | D14-S7.1, D09-S5.2 |
| [DEV-005](spec_deviations/DEV-005.md) | cells on curved sources are not true Voronoi cells | 2026-08-08 | resolved | D09-S5.2, D14-S7.1 |
| [DEV-006](spec_deviations/DEV-006.md) | test_complex_hollow has no internal cross rib and weighs 2127 kg, not 2500 | 2026-08-08 | active | D14-S7.1, D09-S12 |
| [DEV-007](spec_deviations/DEV-007.md) | TestWorld builds its own Bullet world at 0.005 m, not via PhysicsWorld.create | 2026-08-08 | resolved | D14-S5.5, D06-S5.1 |

### DEV-001 — gdx-gltf 3.x does not exist; the pinned version is 2.3.0 from JitPack
D02-S4.1 (D02-R1) named "gdx-gltf 3.x" as the runtime glTF importer. No 3.x release exists; the newest is 2.3.0, and gdx-gltf is published to JitPack only, never to Maven Central. The catalog pins 2.3.0 and D02-S4.1 was amended in the same commit.

### DEV-002 — Cell Fracture is unavailable; cells built by half-space clipping
D09-S5.2 drives Blender's Cell Fracture add-on (`bpy.ops.object.add_fracture_cell_objects`), which wraps voro++. That add-on is not part of Blender 4.2's core distribution — it moved to the extensions platform — and is absent from both a stock install and the `bpy` PyPI module. Cells are constructed directly instead, by clipping a copy of the source mesh with the perpendicular-bisector half-spaces between sites.

### DEV-003 — Hull simplification maximises remaining volume, not minimum volume increase
D09-S5.5 says hull simplification repeatedly drops the vertex whose removal *increases* the hull volume least. That rule is correct for simplifying a concave outline and wrong for a convex hull, where removing any vertex can only shrink it — so as written it selects the single most destructive removal at every step. The implementation maximises the remaining volume instead, then re-inflates to restore enclosure.

### DEV-004 — test_complex_hollow does not fracture; the fixture is unprocessed
**Resolved 2026-08-08 by convex decomposition of the source (DEC-011).** Four of the five D14-S7.1 fixtures process to exit 0. `test_complex_hollow` does not: the shard mass sum comes out at 13571 kg against a true part mass of 2127 kg — 6.4x — so the tool correctly refuses it with `MASS_IMPLAUSIBLE` (72). The fixture is excluded from `:blender-tool:processFixtures` until the cause is fixed. It is now included and processes to exit 0: 14 shards summing to 2127.2 kg, 0.006% from the part mass.

### DEV-005 — cells on curved sources are not true Voronoi cells
**Resolved 2026-08-08 by the exact half-space path.** The bisect-and-fill cell construction of DEV-002 produced exact Voronoi cells on a planar-faced source and materially wrong ones on a curved source: on `test_sphere_r0.5`, 178 of 2400 sampled interior points were closer to a foreign site than to their own cell's, median depth 5.5% of the part's extent. Cells are now built as exact convex polytopes from half-spaces, and all three Voronoi properties hold on every convex fixture including a 100-shard case.

### DEV-006 — test_complex_hollow has no internal cross rib and weighs 2127 kg, not 2500
D14-R20 describes `test_complex_hollow` as a hollow 1 m box with 0.05 m walls **and one internal cross rib**, at ≈ 2500 kg. The checked-in fixture is the shell alone: no rib, 0.2710 m³, 2127.4 kg at steel density. The gap predates this session and was never recorded.

### DEV-007 — TestWorld builds its own Bullet world at 0.005 m, not via PhysicsWorld.create
D14-R10 requires the harness to construct its Bullet world through `game-core`'s `PhysicsWorld.create()`, and D06-R13 fixes the convex collision margin at 0.01 m. `TestWorld` did neither: it built its own world at 0.005 m, on Bullet's default solver settings. **Resolved 2026-08-08** — it delegates to `PhysicsWorld`, and its margin constant is an alias of the game's.

## Session Summaries

| ID | Title | Date | Status | Related Docs |
|---|---|---|---|---|
| [SESS-001](session_summaries/SESS-001.md) | Authored the complete 15-document blueprint suite and operational files | 2026-08-07 | active | D00-S4.2, D13-S4.1 |
| [SESS-002](session_summaries/SESS-002.md) | Scaffolded the Gradle build, guardrail checks, ECS core, and memory tooling | 2026-08-08 | active | D02-S4.4, D04-S4.5, D13-S5.5 |
| [SESS-003](session_summaries/SESS-003.md) | Component catalogue, destruction tool, and verification harness | 2026-08-08 | active | D04-S4.3, D09-S5.1, D14-S5.2 |
| [SESS-004](session_summaries/SESS-004.md) | Exact Voronoi fracture at 100+ shards, and a CI pipeline that runs once | 2026-08-08 | active | D09-S5.3, D12-S5.4, D14-S5 |
| [SESS-005](session_summaries/SESS-005.md) | Convex decomposition of the source; the fifth fixture fractures | 2026-08-08 | active | D09-S5.2, D14-S7.1 |
| [SESS-006](session_summaries/SESS-006.md) | PhysicsSystem — the Bullet step joins the schedule | 2026-08-08 | active | D06-S5.4, D04-S4.4 |
| [SESS-007](session_summaries/SESS-007.md) | the harness runs on the game's physics world | 2026-08-08 | active | D14-S5.5, D06-S4.1, D02-S4.5 |

### SESS-001 — Authored the complete 15-document blueprint suite and operational files
Created the full Syndicate blueprint suite: 15 contractual specification documents in `docs/`, the root operational files `CLAUDE.md` and `JULES.md`, and this `.agent-memory/` structure. No implementation code was written; the repository now holds the contract that future sessions implement against.

### SESS-002 — Scaffolded the Gradle build, guardrail checks, ECS core, and memory tooling
Took the repository from documentation-only to a buildable baseline: an eight-module Gradle build with the pinned toolchain of D02-S4.1, six guardrail check tasks, `shared-models`, the `game-core` ECS engine, and working `.agent-memory` tooling. `./gradlew build check` passes with 65 unit tests green.

### SESS-003 — Component catalogue, destruction tool, and verification harness
Took the repository from "does not compile" to a working destruction pipeline: a mesh goes in, comes out fractured and verified by two independent implementations, and is rendered mid-explosion from the same simulation the checks measured. Cube and sphere captures are in `build/captures/`.

### SESS-004 — Exact Voronoi fracture at 100+ shards, and a CI pipeline that runs once
Replaced the approximate Voronoi cell construction with exact half-space intersection on convex sources, raised the fixture shard counts to 100-200 and captured real mid-explosion renders at that density, then found and fixed the reason a clean clone would not build (DISC-005) and restructured CI so one commit produces one run instead of two.

### SESS-005 — Convex decomposition of the source; the fifth fixture fractures
Resolved DEV-004 by decomposing a non-convex source into disjoint convex pieces, so it reaches the same exact half-space fracture path a convex source does. `test_complex_hollow` went from `MASS_IMPLAUSIBLE` at 6.4x its part mass to exit 0 at 0.006%, and is back in `fixtureSpecs` and in the Voronoi property tests.

### SESS-006 — PhysicsSystem — the Bullet step joins the schedule
Implemented the first of D04-S4.4's 27 systems: `PhysicsSystem` in slot 10, on a new `PhysicsWorld` built to D06-S4.1. The simulation now advances — a body falls, lands, and its `Transform` and `Velocity` components say so.

### SESS-007 — the harness runs on the game's physics world
Resolved DEV-007: `TestWorld` now delegates to `PhysicsWorld.create()` instead of building its own Bullet world at a different collision margin. The blocker turned out to be a single unused dependency, and the fix needed no fixture expectations updated — which is itself the finding.

## Blueprint Coverage

| Doc | Entries referencing it |
|---|---|
| docs/00_master_index.md | DEC-001, DEC-009, PROG-001, SESS-001 |
| docs/02_technical_architecture.md | DEC-006, DEC-007, DEC-008, DEC-010, DEC-012, DEC-013, DEV-001, DISC-001, DISC-005, DISC-007, PROG-001, SESS-002, SESS-007 |
| docs/03_runtime_modes.md | DEC-007 |
| docs/04_entity_component_model.md | DEC-001, DEC-007, DEC-012, PROG-003, SESS-002, SESS-003, SESS-006 |
| docs/05_vehicle_part_system.md | DEC-002, DEC-004 |
| docs/06_physics_simulation.md | DEC-002, DEC-004, DEC-012, DEV-007, DISC-004, PROG-003, SESS-006, SESS-007 |
| docs/07_damage_destruction_model.md | DEC-005 |
| docs/08_asset_pipeline.md | DEC-003, DEV-001 |
| docs/09_blender_destruction_tool.md | DEC-003, DEC-009, DEC-011, DEV-002, DEV-003, DEV-004, DEV-005, DEV-006, DISC-002, DISC-003, DISC-006, PROG-002, SESS-003, SESS-004, SESS-005 |
| docs/10_networking_multiplayer.md | DEC-005 |
| docs/12_testing_validation_ci.md | DEC-006, DEC-010, DISC-001, DISC-005, DISC-007, SESS-004 |
| docs/13_persistent_memory_system.md | SESS-001, SESS-002 |
| docs/14_test_environment.md | DEC-008, DEC-011, DEC-013, DEV-003, DEV-004, DEV-005, DEV-006, DEV-007, DISC-004, PROG-002, SESS-003, SESS-004, SESS-005, SESS-007 |
